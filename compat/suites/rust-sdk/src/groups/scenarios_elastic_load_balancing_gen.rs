// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/scenarios/elastic-load-balancing.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/scenarios/elastic-load-balancing.json";

const GROUP_ELASTIC_LOAD_BALANCING_GEN_LOADBALANCERPOLICY: Group = Group {
    name: "elastic-load-balancing-gen-loadbalancerpolicy",
    file: SCENARIO_FILE,
};

const GROUP_ELASTIC_LOAD_BALANCING_GEN_PROBE: Group = Group {
    name: "elastic-load-balancing-gen-probe",
    file: SCENARIO_FILE,
};

/// The generated elastic-load-balancing groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosElasticLoadBalancing {
    client: aws_sdk_elasticloadbalancing::Client,
}

impl ScenariosElasticLoadBalancing {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_elasticloadbalancing::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_elasticloadbalancing::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosElasticLoadBalancing {
    fn name(&self) -> &'static str {
        "scenarios/elastic-load-balancing"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "elastic-load-balancing-gen-loadbalancerpolicy:CreateLoadBalancerPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ELASTIC_LOAD_BALANCING_GEN_LOADBALANCERPOLICY
                            .run_test(&ctx, "CreateLoadBalancerPolicy", test_elastic_load_balancing_gen_loadbalancerpolicy_create_load_balancer_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "elastic-load-balancing-gen-loadbalancerpolicy:DescribeLoadBalancerPolicies".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ELASTIC_LOAD_BALANCING_GEN_LOADBALANCERPOLICY
                            .run_test(&ctx, "DescribeLoadBalancerPolicies", test_elastic_load_balancing_gen_loadbalancerpolicy_describe_load_balancer_policies(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "elastic-load-balancing-gen-loadbalancerpolicy:DeleteLoadBalancerPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ELASTIC_LOAD_BALANCING_GEN_LOADBALANCERPOLICY
                            .run_test(&ctx, "DeleteLoadBalancerPolicy", test_elastic_load_balancing_gen_loadbalancerpolicy_delete_load_balancer_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "elastic-load-balancing-gen-probe:DescribeAccountLimits".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ELASTIC_LOAD_BALANCING_GEN_PROBE
                            .run_test(&ctx, "DescribeAccountLimits", test_elastic_load_balancing_gen_probe_describe_account_limits(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "elastic-load-balancing-gen-probe:DescribeLoadBalancerAttributes".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ELASTIC_LOAD_BALANCING_GEN_PROBE
                            .run_test(&ctx, "DescribeLoadBalancerAttributes", test_elastic_load_balancing_gen_probe_describe_load_balancer_attributes(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "elastic-load-balancing-gen-probe:DescribeLoadBalancerPolicyTypes".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ELASTIC_LOAD_BALANCING_GEN_PROBE
                            .run_test(&ctx, "DescribeLoadBalancerPolicyTypes", test_elastic_load_balancing_gen_probe_describe_load_balancer_policy_types(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "elastic-load-balancing-gen-probe:DescribeTags".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ELASTIC_LOAD_BALANCING_GEN_PROBE
                            .run_test(&ctx, "DescribeTags", test_elastic_load_balancing_gen_probe_describe_tags(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "elastic-load-balancing-gen-loadbalancerpolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ELASTIC_LOAD_BALANCING_GEN_LOADBALANCERPOLICY.run_setup(&ctx, setup_elastic_load_balancing_gen_loadbalancerpolicy(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            setups.insert(
                "elastic-load-balancing-gen-probe".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ELASTIC_LOAD_BALANCING_GEN_PROBE.run_setup(&ctx, setup_elastic_load_balancing_gen_probe(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "elastic-load-balancing-gen-loadbalancerpolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ELASTIC_LOAD_BALANCING_GEN_LOADBALANCERPOLICY.run_teardown(&ctx, teardown_elastic_load_balancing_gen_loadbalancerpolicy(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            teardowns.insert(
                "elastic-load-balancing-gen-probe".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ELASTIC_LOAD_BALANCING_GEN_PROBE.run_teardown(&ctx, teardown_elastic_load_balancing_gen_probe(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_elastic_load_balancing_gen_loadbalancerpolicy(client: &aws_sdk_elasticloadbalancing::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreateLoadBalancer",
            params: scenario::map(vec![
                ("AvailabilityZones", scenario::lit(::serde_json::json!(["us-east-1a"]))),
                ("Listeners", scenario::lit(::serde_json::json!([{"InstancePort": 80, "InstanceProtocol": "HTTP", "LoadBalancerPort": 80, "Protocol": "HTTP"}]))),
                ("LoadBalancerName", scenario::name("lb")),
            ]),
            export: vec![
                ("loadbalancer.dns", "$.DNSName"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_load_balancer()
                            .availability_zones("us-east-1a")
                            .listeners(
                                aws_sdk_elasticloadbalancing::types::Listener::builder()
                                    .instance_port(80)
                                    .instance_protocol("HTTP")
                                    .load_balancer_port(80)
                                    .protocol("HTTP")
                                    .build()
                                    .map_err(|err| scenario::build_error("Listeners[0]", err))?
                            )
                            .load_balancer_name(b.string("LoadBalancerName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DescribeLoadBalancers",
            params: scenario::map(vec![
                ("LoadBalancerNames", scenario::list(vec![scenario::name("lb")])),
            ]),
            export: vec![
                ("loadbalancer.name", "$.LoadBalancerDescriptions[0].LoadBalancerName"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_load_balancers()
                            .load_balancer_names(b.string("LoadBalancerNames[0]")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_elastic_load_balancing_gen_loadbalancerpolicy(client: &aws_sdk_elasticloadbalancing::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteLoadBalancerPolicy",
            params: scenario::map(vec![
                ("LoadBalancerName", scenario::context("loadbalancer.name")),
                ("PolicyName", scenario::name("policy")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_load_balancer_policy()
                            .load_balancer_name(b.string("LoadBalancerName")?)
                            .policy_name(b.string("PolicyName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteLoadBalancer",
            params: scenario::map(vec![
                ("LoadBalancerName", scenario::context("loadbalancer.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_load_balancer()
                            .load_balancer_name(b.string("LoadBalancerName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_elastic_load_balancing_gen_loadbalancerpolicy_create_load_balancer_policy(client: &aws_sdk_elasticloadbalancing::Client) -> Test {
    Test {
        call: Call {
            op: "CreateLoadBalancerPolicy",
            params: scenario::map(vec![
                ("LoadBalancerName", scenario::context("loadbalancer.name")),
                ("PolicyAttributes", scenario::lit(::serde_json::json!([{"AttributeName": "ProxyProtocol", "AttributeValue": "true"}]))),
                ("PolicyName", scenario::name("policy")),
                ("PolicyTypeName", scenario::lit(::serde_json::json!("ProxyProtocolPolicyType"))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_load_balancer_policy()
                            .load_balancer_name(b.string("LoadBalancerName")?)
                            .policy_attributes(
                                aws_sdk_elasticloadbalancing::types::PolicyAttribute::builder()
                                    .attribute_name("ProxyProtocol")
                                    .attribute_value("true")
                                    .build()
                            )
                            .policy_name(b.string("PolicyName")?)
                            .policy_type_name("ProxyProtocolPolicyType")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::readback(
                Call {
                    op: "DescribeLoadBalancerPolicies",
                    params: scenario::map(vec![
                        ("LoadBalancerName", scenario::context("loadbalancer.name")),
                        ("PolicyNames", scenario::list(vec![scenario::name("policy")])),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .describe_load_balancer_policies()
                                    .load_balancer_name(b.string("LoadBalancerName")?)
                                    .policy_names(b.string("PolicyNames[0]")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::non_empty("$.PolicyDescriptions[0].PolicyName"),
                ],
            ),
        ],
    }
}

fn test_elastic_load_balancing_gen_loadbalancerpolicy_describe_load_balancer_policies(client: &aws_sdk_elasticloadbalancing::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeLoadBalancerPolicies",
            params: scenario::map(vec![
                ("LoadBalancerName", scenario::context("loadbalancer.name")),
                ("PolicyNames", scenario::list(vec![scenario::name("policy")])),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_load_balancer_policies()
                            .load_balancer_name(b.string("LoadBalancerName")?)
                            .policy_names(b.string("PolicyNames[0]")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.PolicyDescriptions[0].PolicyName"),
            ]),
        ],
    }
}

fn test_elastic_load_balancing_gen_loadbalancerpolicy_delete_load_balancer_policy(client: &aws_sdk_elasticloadbalancing::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteLoadBalancerPolicy",
            params: scenario::map(vec![
                ("LoadBalancerName", scenario::context("loadbalancer.name")),
                ("PolicyName", scenario::name("policy")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_load_balancer_policy()
                            .load_balancer_name(b.string("LoadBalancerName")?)
                            .policy_name(b.string("PolicyName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::absent_by_error(
                Call {
                    op: "DescribeLoadBalancerPolicies",
                    params: scenario::map(vec![
                        ("LoadBalancerName", scenario::context("loadbalancer.name")),
                        ("PolicyNames", scenario::list(vec![scenario::name("policy")])),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .describe_load_balancer_policies()
                                    .load_balancer_name(b.string("LoadBalancerName")?)
                                    .policy_names(b.string("PolicyNames[0]")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                scenario::error("PolicyNotFoundException", "PolicyNotFound"),
            ),
        ],
    }
}

fn setup_elastic_load_balancing_gen_probe(_client: &aws_sdk_elasticloadbalancing::Client) -> Vec<Call> {
    // An empty phase is a no-op, not a missing one.
    Vec::new()
}

fn teardown_elastic_load_balancing_gen_probe(_client: &aws_sdk_elasticloadbalancing::Client) -> Vec<Call> {
    // An empty phase is a no-op, not a missing one.
    Vec::new()
}

fn test_elastic_load_balancing_gen_probe_describe_account_limits(client: &aws_sdk_elasticloadbalancing::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeAccountLimits",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_account_limits()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.Limits"),
            ]),
        ],
    }
}

fn test_elastic_load_balancing_gen_probe_describe_load_balancer_attributes(client: &aws_sdk_elasticloadbalancing::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeLoadBalancerAttributes",
            params: scenario::lit(::serde_json::json!({"LoadBalancerName": "compat-scenario-nonexistent"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_load_balancer_attributes()
                            .load_balancer_name("compat-scenario-nonexistent")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.LoadBalancerAttributes"),
            ]),
        ],
    }
}

fn test_elastic_load_balancing_gen_probe_describe_load_balancer_policy_types(client: &aws_sdk_elasticloadbalancing::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeLoadBalancerPolicyTypes",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_load_balancer_policy_types()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.PolicyTypeDescriptions"),
            ]),
        ],
    }
}

fn test_elastic_load_balancing_gen_probe_describe_tags(client: &aws_sdk_elasticloadbalancing::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeTags",
            params: scenario::lit(::serde_json::json!({"LoadBalancerNames": ["compat-scenario-nonexistent"]})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_tags()
                            .load_balancer_names("compat-scenario-nonexistent")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.TagDescriptions"),
            ]),
        ],
    }
}
