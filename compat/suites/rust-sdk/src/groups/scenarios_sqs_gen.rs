// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/scenarios/sqs.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/scenarios/sqs.json";

const GROUP_SQS_GEN_QUEUE: Group = Group {
    name: "sqs-gen-queue",
    file: SCENARIO_FILE,
};

const GROUP_SQS_GEN_MESSAGE: Group = Group {
    name: "sqs-gen-message",
    file: SCENARIO_FILE,
};

const GROUP_SQS_GEN_BATCH: Group = Group {
    name: "sqs-gen-batch",
    file: SCENARIO_FILE,
};

const GROUP_SQS_GEN_PROBE: Group = Group {
    name: "sqs-gen-probe",
    file: SCENARIO_FILE,
};

/// The generated sqs groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosSqs {
    client: aws_sdk_sqs::Client,
}

impl ScenariosSqs {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_sqs::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_sqs::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosSqs {
    fn name(&self) -> &'static str {
        "scenarios/sqs"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "sqs-gen-queue:CreateQueue".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_QUEUE
                            .run_test(&ctx, "CreateQueue", test_sqs_gen_queue_create_queue(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "sqs-gen-queue:GetQueueAttributes".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_QUEUE
                            .run_test(&ctx, "GetQueueAttributes", test_sqs_gen_queue_get_queue_attributes(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "sqs-gen-queue:GetQueueUrl".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_QUEUE
                            .run_test(&ctx, "GetQueueUrl", test_sqs_gen_queue_get_queue_url(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "sqs-gen-queue:SetQueueAttributes".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_QUEUE
                            .run_test(&ctx, "SetQueueAttributes", test_sqs_gen_queue_set_queue_attributes(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "sqs-gen-queue:TagQueue".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_QUEUE
                            .run_test(&ctx, "TagQueue", test_sqs_gen_queue_tag_queue(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "sqs-gen-queue:ListQueueTags".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_QUEUE
                            .run_test(&ctx, "ListQueueTags", test_sqs_gen_queue_list_queue_tags(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "sqs-gen-queue:UntagQueue".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_QUEUE
                            .run_test(&ctx, "UntagQueue", test_sqs_gen_queue_untag_queue(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "sqs-gen-queue:ListDeadLetterSourceQueues".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_QUEUE
                            .run_test(&ctx, "ListDeadLetterSourceQueues", test_sqs_gen_queue_list_dead_letter_source_queues(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "sqs-gen-queue:StartMessageMoveTask".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_QUEUE
                            .run_test(&ctx, "StartMessageMoveTask", test_sqs_gen_queue_start_message_move_task(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "sqs-gen-queue:SendMessageBeforePurge".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_QUEUE
                            .run_test(&ctx, "SendMessageBeforePurge", test_sqs_gen_queue_send_message_before_purge(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "sqs-gen-queue:PurgeQueue".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_QUEUE
                            .run_test(&ctx, "PurgeQueue", test_sqs_gen_queue_purge_queue(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "sqs-gen-queue:ListQueues".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_QUEUE
                            .run_test(&ctx, "ListQueues", test_sqs_gen_queue_list_queues(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "sqs-gen-queue:DeleteQueue".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_QUEUE
                            .run_test(&ctx, "DeleteQueue", test_sqs_gen_queue_delete_queue(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "sqs-gen-message:SendMessage".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_MESSAGE
                            .run_test(&ctx, "SendMessage", test_sqs_gen_message_send_message(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "sqs-gen-message:ReceiveMessage".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_MESSAGE
                            .run_test(&ctx, "ReceiveMessage", test_sqs_gen_message_receive_message(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "sqs-gen-message:ChangeMessageVisibility".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_MESSAGE
                            .run_test(&ctx, "ChangeMessageVisibility", test_sqs_gen_message_change_message_visibility(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "sqs-gen-message:DeleteMessage".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_MESSAGE
                            .run_test(&ctx, "DeleteMessage", test_sqs_gen_message_delete_message(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "sqs-gen-batch:SendMessageBatch".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_BATCH
                            .run_test(&ctx, "SendMessageBatch", test_sqs_gen_batch_send_message_batch(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "sqs-gen-batch:ChangeMessageVisibilityBatch".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_BATCH
                            .run_test(&ctx, "ChangeMessageVisibilityBatch", test_sqs_gen_batch_change_message_visibility_batch(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "sqs-gen-batch:ReceiveMessage".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_BATCH
                            .run_test(&ctx, "ReceiveMessage", test_sqs_gen_batch_receive_message(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "sqs-gen-batch:DeleteMessageBatch".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_BATCH
                            .run_test(&ctx, "DeleteMessageBatch", test_sqs_gen_batch_delete_message_batch(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "sqs-gen-probe:ListMessageMoveTasks".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_PROBE
                            .run_test(&ctx, "ListMessageMoveTasks", test_sqs_gen_probe_list_message_move_tasks(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "sqs-gen-queue".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_QUEUE.run_setup(&ctx, setup_sqs_gen_queue(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            setups.insert(
                "sqs-gen-message".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_MESSAGE.run_setup(&ctx, setup_sqs_gen_message(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            setups.insert(
                "sqs-gen-batch".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_BATCH.run_setup(&ctx, setup_sqs_gen_batch(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            setups.insert(
                "sqs-gen-probe".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_PROBE.run_setup(&ctx, setup_sqs_gen_probe(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "sqs-gen-queue".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_QUEUE.run_teardown(&ctx, teardown_sqs_gen_queue(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            teardowns.insert(
                "sqs-gen-message".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_MESSAGE.run_teardown(&ctx, teardown_sqs_gen_message(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            teardowns.insert(
                "sqs-gen-batch".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_BATCH.run_teardown(&ctx, teardown_sqs_gen_batch(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            teardowns.insert(
                "sqs-gen-probe".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SQS_GEN_PROBE.run_teardown(&ctx, teardown_sqs_gen_probe(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_sqs_gen_queue(client: &aws_sdk_sqs::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreateQueue",
            params: scenario::map(vec![("QueueName", scenario::name("dlq"))]),
            export: vec![
                ("dlq.url", "$.QueueUrl"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_queue()
                            .queue_name(b.string("QueueName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "GetQueueAttributes",
            params: scenario::map(vec![
                ("AttributeNames", scenario::lit(::serde_json::json!(["QueueArn"]))),
                ("QueueUrl", scenario::context("dlq.url")),
            ]),
            export: vec![
                ("dlq.arn", "$.Attributes.QueueArn"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_queue_attributes()
                            .attribute_names(aws_sdk_sqs::types::QueueAttributeName::from("QueueArn"))
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_sqs_gen_queue(client: &aws_sdk_sqs::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteQueue",
            params: scenario::map(vec![
                ("QueueUrl", scenario::context("queue.url")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_queue()
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteQueue",
            params: scenario::map(vec![("QueueUrl", scenario::context("dlq.url"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_queue()
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_sqs_gen_queue_create_queue(client: &aws_sdk_sqs::Client) -> Test {
    Test {
        call: Call {
            op: "CreateQueue",
            params: scenario::map(vec![
                ("Attributes", scenario::map(vec![
                    ("RedrivePolicy", scenario::concat(vec![
                        scenario::lit(::serde_json::json!("{\"deadLetterTargetArn\":\"")),
                        scenario::context("dlq.arn"),
                        scenario::lit(::serde_json::json!("\",\"maxReceiveCount\":\"5\"}")),
                    ])),
                    ("VisibilityTimeout", scenario::lit(::serde_json::json!("30"))),
                ])),
                ("QueueName", scenario::name("q")),
            ]),
            export: vec![
                ("queue.url", "$.QueueUrl"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_queue()
                            .attributes(aws_sdk_sqs::types::QueueAttributeName::from("RedrivePolicy"), b.string("Attributes.RedrivePolicy")?)
                            .attributes(aws_sdk_sqs::types::QueueAttributeName::from("VisibilityTimeout"), "30")
                            .queue_name(b.string("QueueName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.QueueUrl"),
            ]),
            scenario::eventually(30, 2000,
                scenario::readback(
                    Call {
                        op: "GetQueueAttributes",
                        params: scenario::map(vec![
                            ("AttributeNames", scenario::lit(::serde_json::json!(["QueueArn"]))),
                            ("QueueUrl", scenario::context("queue.url")),
                        ]),
                        export: vec![
                            ("queue.arn", "$.Attributes.QueueArn"),
                        ],
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_queue_attributes()
                                        .attribute_names(aws_sdk_sqs::types::QueueAttributeName::from("QueueArn"))
                                        .queue_url(b.string("QueueUrl")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::non_empty("$.Attributes.QueueArn"),
                    ],
                ),
            ),
            scenario::eventually(30, 2000,
                scenario::readback(
                    Call {
                        op: "GetQueueAttributes",
                        params: scenario::map(vec![
                            ("AttributeNames", scenario::lit(::serde_json::json!(["All"]))),
                            ("QueueUrl", scenario::context("queue.url")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_queue_attributes()
                                        .attribute_names(aws_sdk_sqs::types::QueueAttributeName::from("All"))
                                        .queue_url(b.string("QueueUrl")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.Attributes.QueueArn", scenario::context("queue.arn")),
                        scenario::equals("$.Attributes.VisibilityTimeout", scenario::lit(::serde_json::json!("30"))),
                    ],
                ),
            ),
            scenario::eventually(30, 2000,
                scenario::list_contains(
                    Some(Call {
                        op: "ListQueues",
                        params: scenario::map(vec![
                            ("QueueNamePrefix", scenario::name("q")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_queues()
                                        .queue_name_prefix(b.string("QueueNamePrefix")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.QueueUrls",
                    vec![
                        scenario::where_entry("$", scenario::context("queue.url")),
                    ],
                ),
            ),
        ],
    }
}

fn test_sqs_gen_queue_get_queue_attributes(client: &aws_sdk_sqs::Client) -> Test {
    Test {
        call: Call {
            op: "GetQueueAttributes",
            params: scenario::map(vec![
                ("AttributeNames", scenario::lit(::serde_json::json!(["All"]))),
                ("QueueUrl", scenario::context("queue.url")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_queue_attributes()
                            .attribute_names(aws_sdk_sqs::types::QueueAttributeName::from("All"))
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.Attributes.QueueArn", scenario::context("queue.arn")),
            ]),
        ],
    }
}

fn test_sqs_gen_queue_get_queue_url(client: &aws_sdk_sqs::Client) -> Test {
    Test {
        call: Call {
            op: "GetQueueUrl",
            params: scenario::map(vec![("QueueName", scenario::name("q"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_queue_url()
                            .queue_name(b.string("QueueName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.QueueUrl", scenario::context("queue.url")),
            ]),
        ],
    }
}

fn test_sqs_gen_queue_set_queue_attributes(client: &aws_sdk_sqs::Client) -> Test {
    Test {
        call: Call {
            op: "SetQueueAttributes",
            params: scenario::map(vec![
                ("Attributes", scenario::lit(::serde_json::json!({"VisibilityTimeout": "60"}))),
                ("QueueUrl", scenario::context("queue.url")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .set_queue_attributes()
                            .attributes(aws_sdk_sqs::types::QueueAttributeName::from("VisibilityTimeout"), "60")
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(30, 2000,
                scenario::readback(
                    Call {
                        op: "GetQueueAttributes",
                        params: scenario::map(vec![
                            ("AttributeNames", scenario::lit(::serde_json::json!(["All"]))),
                            ("QueueUrl", scenario::context("queue.url")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_queue_attributes()
                                        .attribute_names(aws_sdk_sqs::types::QueueAttributeName::from("All"))
                                        .queue_url(b.string("QueueUrl")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.Attributes.VisibilityTimeout", scenario::lit(::serde_json::json!("60"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_sqs_gen_queue_tag_queue(client: &aws_sdk_sqs::Client) -> Test {
    Test {
        call: Call {
            op: "TagQueue",
            params: scenario::map(vec![
                ("QueueUrl", scenario::context("queue.url")),
                ("Tags", scenario::lit(::serde_json::json!({"compat": "scenario"}))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .tag_queue()
                            .queue_url(b.string("QueueUrl")?)
                            .tags("compat", "scenario")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(30, 2000,
                scenario::readback(
                    Call {
                        op: "ListQueueTags",
                        params: scenario::map(vec![
                            ("QueueUrl", scenario::context("queue.url")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_queue_tags()
                                        .queue_url(b.string("QueueUrl")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.Tags.compat", scenario::lit(::serde_json::json!("scenario"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_sqs_gen_queue_list_queue_tags(client: &aws_sdk_sqs::Client) -> Test {
    Test {
        call: Call {
            op: "ListQueueTags",
            params: scenario::map(vec![
                ("QueueUrl", scenario::context("queue.url")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_queue_tags()
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.Tags.compat", scenario::lit(::serde_json::json!("scenario"))),
            ]),
        ],
    }
}

fn test_sqs_gen_queue_untag_queue(client: &aws_sdk_sqs::Client) -> Test {
    Test {
        call: Call {
            op: "UntagQueue",
            params: scenario::map(vec![
                ("QueueUrl", scenario::context("queue.url")),
                ("TagKeys", scenario::lit(::serde_json::json!(["compat"]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .untag_queue()
                            .queue_url(b.string("QueueUrl")?)
                            .tag_keys("compat")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(30, 2000,
                scenario::readback(
                    Call {
                        op: "ListQueueTags",
                        params: scenario::map(vec![
                            ("QueueUrl", scenario::context("queue.url")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_queue_tags()
                                        .queue_url(b.string("QueueUrl")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::missing("$.Tags.compat"),
                    ],
                ),
            ),
        ],
    }
}

fn test_sqs_gen_queue_list_dead_letter_source_queues(client: &aws_sdk_sqs::Client) -> Test {
    Test {
        call: Call {
            op: "ListDeadLetterSourceQueues",
            params: scenario::map(vec![("QueueUrl", scenario::context("dlq.url"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_dead_letter_source_queues()
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.queueUrls",
                vec![
                    scenario::where_entry("$", scenario::context("queue.url")),
                ],
            ),
        ],
    }
}

fn test_sqs_gen_queue_start_message_move_task(client: &aws_sdk_sqs::Client) -> Test {
    Test {
        call: Call {
            op: "StartMessageMoveTask",
            params: scenario::map(vec![
                ("DestinationArn", scenario::context("queue.arn")),
                ("SourceArn", scenario::context("dlq.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .start_message_move_task()
                            .destination_arn(b.string("DestinationArn")?)
                            .source_arn(b.string("SourceArn")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.TaskHandle"),
            ]),
        ],
    }
}

fn test_sqs_gen_queue_send_message_before_purge(client: &aws_sdk_sqs::Client) -> Test {
    Test {
        call: Call {
            op: "SendMessage",
            params: scenario::map(vec![
                ("MessageBody", scenario::lit(::serde_json::json!("compat scenario message before purge"))),
                ("QueueUrl", scenario::context("queue.url")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .send_message()
                            .message_body("compat scenario message before purge")
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.MessageId"),
            ]),
            scenario::eventually(30, 2000,
                scenario::readback(
                    Call {
                        op: "GetQueueAttributes",
                        params: scenario::map(vec![
                            ("AttributeNames", scenario::lit(::serde_json::json!(["ApproximateNumberOfMessages"]))),
                            ("QueueUrl", scenario::context("queue.url")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_queue_attributes()
                                        .attribute_names(aws_sdk_sqs::types::QueueAttributeName::from("ApproximateNumberOfMessages"))
                                        .queue_url(b.string("QueueUrl")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.Attributes.ApproximateNumberOfMessages", scenario::lit(::serde_json::json!("1"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_sqs_gen_queue_purge_queue(client: &aws_sdk_sqs::Client) -> Test {
    Test {
        call: Call {
            op: "PurgeQueue",
            params: scenario::map(vec![
                ("QueueUrl", scenario::context("queue.url")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .purge_queue()
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(12, 5000,
                scenario::readback(
                    Call {
                        op: "GetQueueAttributes",
                        params: scenario::map(vec![
                            ("AttributeNames", scenario::lit(::serde_json::json!(["ApproximateNumberOfMessages", "ApproximateNumberOfMessagesNotVisible"]))),
                            ("QueueUrl", scenario::context("queue.url")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_queue_attributes()
                                        .attribute_names(aws_sdk_sqs::types::QueueAttributeName::from("ApproximateNumberOfMessages"))
                                        .attribute_names(aws_sdk_sqs::types::QueueAttributeName::from("ApproximateNumberOfMessagesNotVisible"))
                                        .queue_url(b.string("QueueUrl")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.Attributes.ApproximateNumberOfMessages", scenario::lit(::serde_json::json!("0"))),
                        scenario::equals("$.Attributes.ApproximateNumberOfMessagesNotVisible", scenario::lit(::serde_json::json!("0"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_sqs_gen_queue_list_queues(client: &aws_sdk_sqs::Client) -> Test {
    Test {
        call: Call {
            op: "ListQueues",
            params: scenario::map(vec![("QueueNamePrefix", scenario::name("q"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_queues()
                            .queue_name_prefix(b.string("QueueNamePrefix")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.QueueUrls",
                vec![
                    scenario::where_entry("$", scenario::context("queue.url")),
                ],
            ),
        ],
    }
}

fn test_sqs_gen_queue_delete_queue(client: &aws_sdk_sqs::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteQueue",
            params: scenario::map(vec![
                ("QueueUrl", scenario::context("queue.url")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_queue()
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(30, 2000,
                scenario::absent_by_error(
                    Call {
                        op: "GetQueueAttributes",
                        params: scenario::map(vec![
                            ("AttributeNames", scenario::lit(::serde_json::json!(["All"]))),
                            ("QueueUrl", scenario::context("queue.url")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_queue_attributes()
                                        .attribute_names(aws_sdk_sqs::types::QueueAttributeName::from("All"))
                                        .queue_url(b.string("QueueUrl")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    scenario::error("QueueDoesNotExist", "AWS.SimpleQueueService.NonExistentQueue"),
                ),
            ),
        ],
    }
}

fn setup_sqs_gen_message(client: &aws_sdk_sqs::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreateQueue",
            params: scenario::map(vec![("QueueName", scenario::name("dlq"))]),
            export: vec![
                ("dlq.url", "$.QueueUrl"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_queue()
                            .queue_name(b.string("QueueName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "GetQueueAttributes",
            params: scenario::map(vec![
                ("AttributeNames", scenario::lit(::serde_json::json!(["QueueArn"]))),
                ("QueueUrl", scenario::context("dlq.url")),
            ]),
            export: vec![
                ("dlq.arn", "$.Attributes.QueueArn"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_queue_attributes()
                            .attribute_names(aws_sdk_sqs::types::QueueAttributeName::from("QueueArn"))
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "CreateQueue",
            params: scenario::map(vec![
                ("Attributes", scenario::map(vec![
                    ("RedrivePolicy", scenario::concat(vec![
                        scenario::lit(::serde_json::json!("{\"deadLetterTargetArn\":\"")),
                        scenario::context("dlq.arn"),
                        scenario::lit(::serde_json::json!("\",\"maxReceiveCount\":\"5\"}")),
                    ])),
                    ("VisibilityTimeout", scenario::lit(::serde_json::json!("30"))),
                ])),
                ("QueueName", scenario::name("q")),
            ]),
            export: vec![
                ("queue.url", "$.QueueUrl"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_queue()
                            .attributes(aws_sdk_sqs::types::QueueAttributeName::from("RedrivePolicy"), b.string("Attributes.RedrivePolicy")?)
                            .attributes(aws_sdk_sqs::types::QueueAttributeName::from("VisibilityTimeout"), "30")
                            .queue_name(b.string("QueueName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "GetQueueAttributes",
            params: scenario::map(vec![
                ("AttributeNames", scenario::lit(::serde_json::json!(["QueueArn"]))),
                ("QueueUrl", scenario::context("queue.url")),
            ]),
            export: vec![
                ("queue.arn", "$.Attributes.QueueArn"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_queue_attributes()
                            .attribute_names(aws_sdk_sqs::types::QueueAttributeName::from("QueueArn"))
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_sqs_gen_message(client: &aws_sdk_sqs::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteMessage",
            params: scenario::map(vec![
                ("QueueUrl", scenario::context("queue.url")),
                ("ReceiptHandle", scenario::context("message.receiptHandle")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_message()
                            .queue_url(b.string("QueueUrl")?)
                            .receipt_handle(b.string("ReceiptHandle")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteQueue",
            params: scenario::map(vec![
                ("QueueUrl", scenario::context("queue.url")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_queue()
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteQueue",
            params: scenario::map(vec![("QueueUrl", scenario::context("dlq.url"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_queue()
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_sqs_gen_message_send_message(client: &aws_sdk_sqs::Client) -> Test {
    Test {
        call: Call {
            op: "SendMessage",
            params: scenario::map(vec![
                ("MessageBody", scenario::lit(::serde_json::json!("compat scenario message"))),
                ("QueueUrl", scenario::context("queue.url")),
            ]),
            export: vec![
                ("message.id", "$.MessageId"),
                ("message.md5", "$.MD5OfMessageBody"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .send_message()
                            .message_body("compat scenario message")
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.MD5OfMessageBody"),
                scenario::non_empty("$.MessageId"),
            ]),
            scenario::eventually(5, 1000,
                scenario::readback(
                    Call {
                        op: "ReceiveMessage",
                        params: scenario::map(vec![
                            ("MaxNumberOfMessages", scenario::lit(::serde_json::json!(1))),
                            ("QueueUrl", scenario::context("queue.url")),
                            ("VisibilityTimeout", scenario::lit(::serde_json::json!(1))),
                            ("WaitTimeSeconds", scenario::lit(::serde_json::json!(5))),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .receive_message()
                                        .max_number_of_messages(1)
                                        .queue_url(b.string("QueueUrl")?)
                                        .visibility_timeout(1)
                                        .wait_time_seconds(5)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.Messages[0].Body", scenario::lit(::serde_json::json!("compat scenario message"))),
                        scenario::equals("$.Messages[0].MessageId", scenario::context("message.id")),
                    ],
                ),
            ),
        ],
    }
}

fn test_sqs_gen_message_receive_message(client: &aws_sdk_sqs::Client) -> Test {
    Test {
        call: Call {
            op: "ReceiveMessage",
            params: scenario::map(vec![
                ("MaxNumberOfMessages", scenario::lit(::serde_json::json!(1))),
                ("QueueUrl", scenario::context("queue.url")),
                ("WaitTimeSeconds", scenario::lit(::serde_json::json!(5))),
            ]),
            export: vec![
                ("message.receiptHandle", "$.Messages[0].ReceiptHandle"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .receive_message()
                            .max_number_of_messages(1)
                            .queue_url(b.string("QueueUrl")?)
                            .wait_time_seconds(5)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.Messages[0].MessageId", scenario::context("message.id")),
            ]),
        ],
    }
}

fn test_sqs_gen_message_change_message_visibility(client: &aws_sdk_sqs::Client) -> Test {
    Test {
        call: Call {
            op: "ChangeMessageVisibility",
            params: scenario::map(vec![
                ("QueueUrl", scenario::context("queue.url")),
                ("ReceiptHandle", scenario::context("message.receiptHandle")),
                ("VisibilityTimeout", scenario::lit(::serde_json::json!(1))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .change_message_visibility()
                            .queue_url(b.string("QueueUrl")?)
                            .receipt_handle(b.string("ReceiptHandle")?)
                            .visibility_timeout(1)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(5, 1000,
                scenario::readback(
                    Call {
                        op: "ReceiveMessage",
                        params: scenario::map(vec![
                            ("MaxNumberOfMessages", scenario::lit(::serde_json::json!(1))),
                            ("QueueUrl", scenario::context("queue.url")),
                            ("VisibilityTimeout", scenario::lit(::serde_json::json!(1))),
                            ("WaitTimeSeconds", scenario::lit(::serde_json::json!(5))),
                        ]),
                        export: vec![
                            ("message.receiptHandle", "$.Messages[0].ReceiptHandle"),
                        ],
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .receive_message()
                                        .max_number_of_messages(1)
                                        .queue_url(b.string("QueueUrl")?)
                                        .visibility_timeout(1)
                                        .wait_time_seconds(5)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.Messages[0].MessageId", scenario::context("message.id")),
                    ],
                ),
            ),
        ],
    }
}

fn test_sqs_gen_message_delete_message(client: &aws_sdk_sqs::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteMessage",
            params: scenario::map(vec![
                ("QueueUrl", scenario::context("queue.url")),
                ("ReceiptHandle", scenario::context("message.receiptHandle")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_message()
                            .queue_url(b.string("QueueUrl")?)
                            .receipt_handle(b.string("ReceiptHandle")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(5, 1000,
                scenario::absent_from_list(
                    Some(Call {
                        op: "ReceiveMessage",
                        params: scenario::map(vec![
                            ("MaxNumberOfMessages", scenario::lit(::serde_json::json!(10))),
                            ("QueueUrl", scenario::context("queue.url")),
                            ("VisibilityTimeout", scenario::lit(::serde_json::json!(1))),
                            ("WaitTimeSeconds", scenario::lit(::serde_json::json!(1))),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .receive_message()
                                        .max_number_of_messages(10)
                                        .queue_url(b.string("QueueUrl")?)
                                        .visibility_timeout(1)
                                        .wait_time_seconds(1)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Messages",
                    vec![
                        scenario::where_entry("$.MessageId", scenario::context("message.id")),
                    ],
                ),
            ),
        ],
    }
}

fn setup_sqs_gen_batch(client: &aws_sdk_sqs::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreateQueue",
            params: scenario::map(vec![("QueueName", scenario::name("dlq"))]),
            export: vec![
                ("dlq.url", "$.QueueUrl"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_queue()
                            .queue_name(b.string("QueueName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "GetQueueAttributes",
            params: scenario::map(vec![
                ("AttributeNames", scenario::lit(::serde_json::json!(["QueueArn"]))),
                ("QueueUrl", scenario::context("dlq.url")),
            ]),
            export: vec![
                ("dlq.arn", "$.Attributes.QueueArn"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_queue_attributes()
                            .attribute_names(aws_sdk_sqs::types::QueueAttributeName::from("QueueArn"))
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "CreateQueue",
            params: scenario::map(vec![
                ("Attributes", scenario::map(vec![
                    ("RedrivePolicy", scenario::concat(vec![
                        scenario::lit(::serde_json::json!("{\"deadLetterTargetArn\":\"")),
                        scenario::context("dlq.arn"),
                        scenario::lit(::serde_json::json!("\",\"maxReceiveCount\":\"5\"}")),
                    ])),
                    ("VisibilityTimeout", scenario::lit(::serde_json::json!("30"))),
                ])),
                ("QueueName", scenario::name("q")),
            ]),
            export: vec![
                ("queue.url", "$.QueueUrl"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_queue()
                            .attributes(aws_sdk_sqs::types::QueueAttributeName::from("RedrivePolicy"), b.string("Attributes.RedrivePolicy")?)
                            .attributes(aws_sdk_sqs::types::QueueAttributeName::from("VisibilityTimeout"), "30")
                            .queue_name(b.string("QueueName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "GetQueueAttributes",
            params: scenario::map(vec![
                ("AttributeNames", scenario::lit(::serde_json::json!(["QueueArn"]))),
                ("QueueUrl", scenario::context("queue.url")),
            ]),
            export: vec![
                ("queue.arn", "$.Attributes.QueueArn"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_queue_attributes()
                            .attribute_names(aws_sdk_sqs::types::QueueAttributeName::from("QueueArn"))
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_sqs_gen_batch(client: &aws_sdk_sqs::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteMessageBatch",
            params: scenario::map(vec![
                ("Entries", scenario::list(vec![
                    scenario::map(vec![
                        ("Id", scenario::lit(::serde_json::json!("1"))),
                        ("ReceiptHandle", scenario::context("batch.receiptHandle")),
                    ]),
                ])),
                ("QueueUrl", scenario::context("queue.url")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_message_batch()
                            .entries(
                                aws_sdk_sqs::types::DeleteMessageBatchRequestEntry::builder()
                                    .id("1")
                                    .receipt_handle(b.string("Entries[0].ReceiptHandle")?)
                                    .build()
                                    .map_err(|err| scenario::build_error("Entries[0]", err))?
                            )
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteQueue",
            params: scenario::map(vec![
                ("QueueUrl", scenario::context("queue.url")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_queue()
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteQueue",
            params: scenario::map(vec![("QueueUrl", scenario::context("dlq.url"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_queue()
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_sqs_gen_batch_send_message_batch(client: &aws_sdk_sqs::Client) -> Test {
    Test {
        call: Call {
            op: "SendMessageBatch",
            params: scenario::map(vec![
                ("Entries", scenario::lit(::serde_json::json!([{"Id": "1", "MessageBody": "compat batch message 1"}, {"Id": "2", "MessageBody": "compat batch message 2"}]))),
                ("QueueUrl", scenario::context("queue.url")),
            ]),
            export: vec![
                ("batch.firstId", "$.Successful[0].MessageId"),
                ("batch.secondId", "$.Successful[1].MessageId"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .send_message_batch()
                            .entries(
                                aws_sdk_sqs::types::SendMessageBatchRequestEntry::builder()
                                    .id("1")
                                    .message_body("compat batch message 1")
                                    .build()
                                    .map_err(|err| scenario::build_error("Entries[0]", err))?
                            )
                            .entries(
                                aws_sdk_sqs::types::SendMessageBatchRequestEntry::builder()
                                    .id("2")
                                    .message_body("compat batch message 2")
                                    .build()
                                    .map_err(|err| scenario::build_error("Entries[1]", err))?
                            )
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.Successful[0].MessageId"),
                scenario::non_empty("$.Successful[1].MessageId"),
            ]),
            scenario::eventually(5, 1000,
                scenario::readback(
                    Call {
                        op: "ReceiveMessage",
                        params: scenario::map(vec![
                            ("MaxNumberOfMessages", scenario::lit(::serde_json::json!(10))),
                            ("QueueUrl", scenario::context("queue.url")),
                            ("WaitTimeSeconds", scenario::lit(::serde_json::json!(5))),
                        ]),
                        export: vec![
                            ("batch.id", "$.Messages[0].MessageId"),
                            ("batch.receiptHandle", "$.Messages[0].ReceiptHandle"),
                        ],
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .receive_message()
                                        .max_number_of_messages(10)
                                        .queue_url(b.string("QueueUrl")?)
                                        .wait_time_seconds(5)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::non_empty("$.Messages[0].ReceiptHandle"),
                    ],
                ),
            ),
        ],
    }
}

fn test_sqs_gen_batch_change_message_visibility_batch(client: &aws_sdk_sqs::Client) -> Test {
    Test {
        call: Call {
            op: "ChangeMessageVisibilityBatch",
            params: scenario::map(vec![
                ("Entries", scenario::list(vec![
                    scenario::map(vec![
                        ("Id", scenario::lit(::serde_json::json!("1"))),
                        ("ReceiptHandle", scenario::context("batch.receiptHandle")),
                        ("VisibilityTimeout", scenario::lit(::serde_json::json!(1))),
                    ]),
                ])),
                ("QueueUrl", scenario::context("queue.url")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .change_message_visibility_batch()
                            .entries(
                                aws_sdk_sqs::types::ChangeMessageVisibilityBatchRequestEntry::builder()
                                    .id("1")
                                    .receipt_handle(b.string("Entries[0].ReceiptHandle")?)
                                    .visibility_timeout(1)
                                    .build()
                                    .map_err(|err| scenario::build_error("Entries[0]", err))?
                            )
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.Successful[0].Id", scenario::lit(::serde_json::json!("1"))),
            ]),
            scenario::eventually(5, 1000,
                scenario::readback(
                    Call {
                        op: "ReceiveMessage",
                        params: scenario::map(vec![
                            ("MaxNumberOfMessages", scenario::lit(::serde_json::json!(10))),
                            ("QueueUrl", scenario::context("queue.url")),
                            ("VisibilityTimeout", scenario::lit(::serde_json::json!(1))),
                            ("WaitTimeSeconds", scenario::lit(::serde_json::json!(5))),
                        ]),
                        export: vec![
                            ("batch.receiptHandle", "$.Messages[0].ReceiptHandle"),
                        ],
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .receive_message()
                                        .max_number_of_messages(10)
                                        .queue_url(b.string("QueueUrl")?)
                                        .visibility_timeout(1)
                                        .wait_time_seconds(5)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.Messages[0].MessageId", scenario::context("batch.id")),
                    ],
                ),
            ),
        ],
    }
}

fn test_sqs_gen_batch_receive_message(client: &aws_sdk_sqs::Client) -> Test {
    Test {
        call: Call {
            op: "ReceiveMessage",
            params: scenario::map(vec![
                ("MaxNumberOfMessages", scenario::lit(::serde_json::json!(10))),
                ("QueueUrl", scenario::context("queue.url")),
                ("VisibilityTimeout", scenario::lit(::serde_json::json!(1))),
                ("WaitTimeSeconds", scenario::lit(::serde_json::json!(5))),
            ]),
            export: vec![
                ("batch.receiptHandle", "$.Messages[0].ReceiptHandle"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .receive_message()
                            .max_number_of_messages(10)
                            .queue_url(b.string("QueueUrl")?)
                            .visibility_timeout(1)
                            .wait_time_seconds(5)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Messages",
                vec![
                    scenario::where_entry("$.MessageId", scenario::context("batch.id")),
                ],
            ),
        ],
    }
}

fn test_sqs_gen_batch_delete_message_batch(client: &aws_sdk_sqs::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteMessageBatch",
            params: scenario::map(vec![
                ("Entries", scenario::list(vec![
                    scenario::map(vec![
                        ("Id", scenario::lit(::serde_json::json!("1"))),
                        ("ReceiptHandle", scenario::context("batch.receiptHandle")),
                    ]),
                ])),
                ("QueueUrl", scenario::context("queue.url")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_message_batch()
                            .entries(
                                aws_sdk_sqs::types::DeleteMessageBatchRequestEntry::builder()
                                    .id("1")
                                    .receipt_handle(b.string("Entries[0].ReceiptHandle")?)
                                    .build()
                                    .map_err(|err| scenario::build_error("Entries[0]", err))?
                            )
                            .queue_url(b.string("QueueUrl")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(5, 1000,
                scenario::absent_from_list(
                    Some(Call {
                        op: "ReceiveMessage",
                        params: scenario::map(vec![
                            ("MaxNumberOfMessages", scenario::lit(::serde_json::json!(10))),
                            ("QueueUrl", scenario::context("queue.url")),
                            ("VisibilityTimeout", scenario::lit(::serde_json::json!(1))),
                            ("WaitTimeSeconds", scenario::lit(::serde_json::json!(5))),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .receive_message()
                                        .max_number_of_messages(10)
                                        .queue_url(b.string("QueueUrl")?)
                                        .visibility_timeout(1)
                                        .wait_time_seconds(5)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Messages",
                    vec![
                        scenario::where_entry("$.MessageId", scenario::context("batch.id")),
                    ],
                ),
            ),
        ],
    }
}

fn setup_sqs_gen_probe(_client: &aws_sdk_sqs::Client) -> Vec<Call> {
    // An empty phase is a no-op, not a missing one.
    Vec::new()
}

fn teardown_sqs_gen_probe(_client: &aws_sdk_sqs::Client) -> Vec<Call> {
    // An empty phase is a no-op, not a missing one.
    Vec::new()
}

fn test_sqs_gen_probe_list_message_move_tasks(client: &aws_sdk_sqs::Client) -> Test {
    Test {
        call: Call {
            op: "ListMessageMoveTasks",
            params: scenario::lit(::serde_json::json!({"SourceArn": "arn:aws:sqs:us-east-1:123456789012:compat-scenario-nonexistent"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_message_move_tasks()
                            .source_arn("arn:aws:sqs:us-east-1:123456789012:compat-scenario-nonexistent")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.Results"),
            ]),
        ],
    }
}
