// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/scenarios/batch.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/scenarios/batch.json";

const GROUP_BATCH_GEN_SCHEDULINGPOLICY: Group = Group {
    name: "batch-gen-schedulingpolicy",
    file: SCENARIO_FILE,
};

const GROUP_BATCH_GEN_CONSUMABLERESOURCE: Group = Group {
    name: "batch-gen-consumableresource",
    file: SCENARIO_FILE,
};

const GROUP_BATCH_GEN_COMPUTEENVIRONMENT: Group = Group {
    name: "batch-gen-computeenvironment",
    file: SCENARIO_FILE,
};

const GROUP_BATCH_GEN_JOBQUEUE: Group = Group {
    name: "batch-gen-jobqueue",
    file: SCENARIO_FILE,
};

const GROUP_BATCH_GEN_PROBE: Group = Group {
    name: "batch-gen-probe",
    file: SCENARIO_FILE,
};

/// The generated batch groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosBatch {
    client: aws_sdk_batch::Client,
}

impl ScenariosBatch {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_batch::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_batch::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosBatch {
    fn name(&self) -> &'static str {
        "scenarios/batch"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-schedulingpolicy:CreateSchedulingPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_SCHEDULINGPOLICY
                            .run_test(&ctx, "CreateSchedulingPolicy", test_batch_gen_schedulingpolicy_create_scheduling_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-schedulingpolicy:DescribeSchedulingPolicies".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_SCHEDULINGPOLICY
                            .run_test(&ctx, "DescribeSchedulingPolicies", test_batch_gen_schedulingpolicy_describe_scheduling_policies(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-schedulingpolicy:UpdateSchedulingPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_SCHEDULINGPOLICY
                            .run_test(&ctx, "UpdateSchedulingPolicy", test_batch_gen_schedulingpolicy_update_scheduling_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-schedulingpolicy:TagResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_SCHEDULINGPOLICY
                            .run_test(&ctx, "TagResource", test_batch_gen_schedulingpolicy_tag_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-schedulingpolicy:ListTagsForResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_SCHEDULINGPOLICY
                            .run_test(&ctx, "ListTagsForResource", test_batch_gen_schedulingpolicy_list_tags_for_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-schedulingpolicy:UntagResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_SCHEDULINGPOLICY
                            .run_test(&ctx, "UntagResource", test_batch_gen_schedulingpolicy_untag_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-schedulingpolicy:ListSchedulingPolicies".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_SCHEDULINGPOLICY
                            .run_test(&ctx, "ListSchedulingPolicies", test_batch_gen_schedulingpolicy_list_scheduling_policies(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-schedulingpolicy:DeleteSchedulingPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_SCHEDULINGPOLICY
                            .run_test(&ctx, "DeleteSchedulingPolicy", test_batch_gen_schedulingpolicy_delete_scheduling_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-consumableresource:CreateConsumableResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_CONSUMABLERESOURCE
                            .run_test(&ctx, "CreateConsumableResource", test_batch_gen_consumableresource_create_consumable_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-consumableresource:DescribeConsumableResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_CONSUMABLERESOURCE
                            .run_test(&ctx, "DescribeConsumableResource", test_batch_gen_consumableresource_describe_consumable_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-consumableresource:UpdateConsumableResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_CONSUMABLERESOURCE
                            .run_test(&ctx, "UpdateConsumableResource", test_batch_gen_consumableresource_update_consumable_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-consumableresource:TagResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_CONSUMABLERESOURCE
                            .run_test(&ctx, "TagResource", test_batch_gen_consumableresource_tag_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-consumableresource:ListTagsForResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_CONSUMABLERESOURCE
                            .run_test(&ctx, "ListTagsForResource", test_batch_gen_consumableresource_list_tags_for_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-consumableresource:UntagResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_CONSUMABLERESOURCE
                            .run_test(&ctx, "UntagResource", test_batch_gen_consumableresource_untag_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-consumableresource:ListConsumableResources".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_CONSUMABLERESOURCE
                            .run_test(&ctx, "ListConsumableResources", test_batch_gen_consumableresource_list_consumable_resources(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-consumableresource:DeleteConsumableResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_CONSUMABLERESOURCE
                            .run_test(&ctx, "DeleteConsumableResource", test_batch_gen_consumableresource_delete_consumable_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-computeenvironment:CreateComputeEnvironment".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_COMPUTEENVIRONMENT
                            .run_test(&ctx, "CreateComputeEnvironment", test_batch_gen_computeenvironment_create_compute_environment(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-computeenvironment:DescribeComputeEnvironments".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_COMPUTEENVIRONMENT
                            .run_test(&ctx, "DescribeComputeEnvironments", test_batch_gen_computeenvironment_describe_compute_environments(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-computeenvironment:UpdateComputeEnvironment".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_COMPUTEENVIRONMENT
                            .run_test(&ctx, "UpdateComputeEnvironment", test_batch_gen_computeenvironment_update_compute_environment(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-computeenvironment:TagResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_COMPUTEENVIRONMENT
                            .run_test(&ctx, "TagResource", test_batch_gen_computeenvironment_tag_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-computeenvironment:ListTagsForResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_COMPUTEENVIRONMENT
                            .run_test(&ctx, "ListTagsForResource", test_batch_gen_computeenvironment_list_tags_for_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-computeenvironment:UntagResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_COMPUTEENVIRONMENT
                            .run_test(&ctx, "UntagResource", test_batch_gen_computeenvironment_untag_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-computeenvironment:DeleteComputeEnvironment".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_COMPUTEENVIRONMENT
                            .run_test(&ctx, "DeleteComputeEnvironment", test_batch_gen_computeenvironment_delete_compute_environment(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-jobqueue:CreateJobQueue".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_JOBQUEUE
                            .run_test(&ctx, "CreateJobQueue", test_batch_gen_jobqueue_create_job_queue(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-jobqueue:DescribeJobQueues".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_JOBQUEUE
                            .run_test(&ctx, "DescribeJobQueues", test_batch_gen_jobqueue_describe_job_queues(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-jobqueue:UpdateJobQueue".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_JOBQUEUE
                            .run_test(&ctx, "UpdateJobQueue", test_batch_gen_jobqueue_update_job_queue(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-jobqueue:TagResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_JOBQUEUE
                            .run_test(&ctx, "TagResource", test_batch_gen_jobqueue_tag_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-jobqueue:ListTagsForResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_JOBQUEUE
                            .run_test(&ctx, "ListTagsForResource", test_batch_gen_jobqueue_list_tags_for_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-jobqueue:UntagResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_JOBQUEUE
                            .run_test(&ctx, "UntagResource", test_batch_gen_jobqueue_untag_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-jobqueue:GetJobQueueSnapshot".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_JOBQUEUE
                            .run_test(&ctx, "GetJobQueueSnapshot", test_batch_gen_jobqueue_get_job_queue_snapshot(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-jobqueue:ListJobs".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_JOBQUEUE
                            .run_test(&ctx, "ListJobs", test_batch_gen_jobqueue_list_jobs(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-jobqueue:DeleteJobQueue".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_JOBQUEUE
                            .run_test(&ctx, "DeleteJobQueue", test_batch_gen_jobqueue_delete_job_queue(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-probe:DescribeJobs".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_PROBE
                            .run_test(&ctx, "DescribeJobs", test_batch_gen_probe_describe_jobs(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-probe:DescribeQuotaShare".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_PROBE
                            .run_test(&ctx, "DescribeQuotaShare", test_batch_gen_probe_describe_quota_share(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-probe:DescribeServiceEnvironments".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_PROBE
                            .run_test(&ctx, "DescribeServiceEnvironments", test_batch_gen_probe_describe_service_environments(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-probe:DescribeServiceJob".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_PROBE
                            .run_test(&ctx, "DescribeServiceJob", test_batch_gen_probe_describe_service_job(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-probe:ListJobsByConsumableResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_PROBE
                            .run_test(&ctx, "ListJobsByConsumableResource", test_batch_gen_probe_list_jobs_by_consumable_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-probe:ListQuotaShares".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_PROBE
                            .run_test(&ctx, "ListQuotaShares", test_batch_gen_probe_list_quota_shares(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "batch-gen-probe:ListServiceJobs".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_PROBE
                            .run_test(&ctx, "ListServiceJobs", test_batch_gen_probe_list_service_jobs(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "batch-gen-schedulingpolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_SCHEDULINGPOLICY.run_setup(&ctx, setup_batch_gen_schedulingpolicy(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            setups.insert(
                "batch-gen-consumableresource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_CONSUMABLERESOURCE.run_setup(&ctx, setup_batch_gen_consumableresource(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            setups.insert(
                "batch-gen-computeenvironment".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_COMPUTEENVIRONMENT.run_setup(&ctx, setup_batch_gen_computeenvironment(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            setups.insert(
                "batch-gen-jobqueue".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_JOBQUEUE.run_setup(&ctx, setup_batch_gen_jobqueue(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            setups.insert(
                "batch-gen-probe".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_PROBE.run_setup(&ctx, setup_batch_gen_probe(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "batch-gen-schedulingpolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_SCHEDULINGPOLICY.run_teardown(&ctx, teardown_batch_gen_schedulingpolicy(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            teardowns.insert(
                "batch-gen-consumableresource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_CONSUMABLERESOURCE.run_teardown(&ctx, teardown_batch_gen_consumableresource(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            teardowns.insert(
                "batch-gen-computeenvironment".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_COMPUTEENVIRONMENT.run_teardown(&ctx, teardown_batch_gen_computeenvironment(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            teardowns.insert(
                "batch-gen-jobqueue".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_JOBQUEUE.run_teardown(&ctx, teardown_batch_gen_jobqueue(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            teardowns.insert(
                "batch-gen-probe".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BATCH_GEN_PROBE.run_teardown(&ctx, teardown_batch_gen_probe(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_batch_gen_schedulingpolicy(_client: &aws_sdk_batch::Client) -> Vec<Call> {
    // An empty phase is a no-op, not a missing one.
    Vec::new()
}

fn teardown_batch_gen_schedulingpolicy(client: &aws_sdk_batch::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteSchedulingPolicy",
            params: scenario::map(vec![
                ("arn", scenario::context("schedulingpolicy.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_scheduling_policy()
                            .arn(b.string("arn")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_batch_gen_schedulingpolicy_create_scheduling_policy(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "CreateSchedulingPolicy",
            params: scenario::map(vec![
                ("fairsharePolicy", scenario::lit(::serde_json::json!({"shareDecaySeconds": 100}))),
                ("name", scenario::name("sp")),
            ]),
            export: vec![
                ("schedulingpolicy.arn", "$.arn"),
                ("schedulingpolicy.name", "$.name"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_scheduling_policy()
                            .fairshare_policy(
                                aws_sdk_batch::types::FairsharePolicy::builder()
                                    .share_decay_seconds(100)
                                    .build()
                            )
                            .name(b.string("name")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.arn"),
                scenario::non_empty("$.name"),
            ]),
            scenario::readback(
                Call {
                    op: "DescribeSchedulingPolicies",
                    params: scenario::map(vec![
                        ("arns", scenario::list(vec![
                            scenario::context("schedulingpolicy.arn"),
                        ])),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .describe_scheduling_policies()
                                    .arns(b.string("arns[0]")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.schedulingPolicies[0].arn", scenario::context("schedulingpolicy.arn")),
                    scenario::equals("$.schedulingPolicies[0].fairsharePolicy.shareDecaySeconds", scenario::lit(::serde_json::json!(100))),
                ],
            ),
            scenario::list_contains(
                Some(Call {
                    op: "ListSchedulingPolicies",
                    params: scenario::lit(::serde_json::json!({})),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |_b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_scheduling_policies()
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.schedulingPolicies",
                vec![
                    scenario::where_entry("$.arn", scenario::context("schedulingpolicy.arn")),
                ],
            ),
        ],
    }
}

fn test_batch_gen_schedulingpolicy_describe_scheduling_policies(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeSchedulingPolicies",
            params: scenario::map(vec![
                ("arns", scenario::list(vec![
                    scenario::context("schedulingpolicy.arn"),
                ])),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_scheduling_policies()
                            .arns(b.string("arns[0]")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.schedulingPolicies[0].arn", scenario::context("schedulingpolicy.arn")),
            ]),
        ],
    }
}

fn test_batch_gen_schedulingpolicy_update_scheduling_policy(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "UpdateSchedulingPolicy",
            params: scenario::map(vec![
                ("arn", scenario::context("schedulingpolicy.arn")),
                ("fairsharePolicy", scenario::lit(::serde_json::json!({"shareDecaySeconds": 300}))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .update_scheduling_policy()
                            .arn(b.string("arn")?)
                            .fairshare_policy(
                                aws_sdk_batch::types::FairsharePolicy::builder()
                                    .share_decay_seconds(300)
                                    .build()
                            )
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::readback(
                Call {
                    op: "DescribeSchedulingPolicies",
                    params: scenario::map(vec![
                        ("arns", scenario::list(vec![
                            scenario::context("schedulingpolicy.arn"),
                        ])),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .describe_scheduling_policies()
                                    .arns(b.string("arns[0]")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.schedulingPolicies[0].fairsharePolicy.shareDecaySeconds", scenario::lit(::serde_json::json!(300))),
                ],
            ),
        ],
    }
}

fn test_batch_gen_schedulingpolicy_tag_resource(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "TagResource",
            params: scenario::map(vec![
                ("resourceArn", scenario::context("schedulingpolicy.arn")),
                ("tags", scenario::lit(::serde_json::json!({"compat": "scenario"}))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .tag_resource()
                            .resource_arn(b.string("resourceArn")?)
                            .tags("compat", "scenario")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::readback(
                Call {
                    op: "ListTagsForResource",
                    params: scenario::map(vec![
                        ("resourceArn", scenario::context("schedulingpolicy.arn")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_tags_for_resource()
                                    .resource_arn(b.string("resourceArn")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.tags.compat", scenario::lit(::serde_json::json!("scenario"))),
                ],
            ),
        ],
    }
}

fn test_batch_gen_schedulingpolicy_list_tags_for_resource(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "ListTagsForResource",
            params: scenario::map(vec![
                ("resourceArn", scenario::context("schedulingpolicy.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_tags_for_resource()
                            .resource_arn(b.string("resourceArn")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.tags.compat", scenario::lit(::serde_json::json!("scenario"))),
            ]),
        ],
    }
}

fn test_batch_gen_schedulingpolicy_untag_resource(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "UntagResource",
            params: scenario::map(vec![
                ("resourceArn", scenario::context("schedulingpolicy.arn")),
                ("tagKeys", scenario::lit(::serde_json::json!(["compat"]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .untag_resource()
                            .resource_arn(b.string("resourceArn")?)
                            .tag_keys("compat")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::readback(
                Call {
                    op: "ListTagsForResource",
                    params: scenario::map(vec![
                        ("resourceArn", scenario::context("schedulingpolicy.arn")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_tags_for_resource()
                                    .resource_arn(b.string("resourceArn")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::missing("$.tags.compat"),
                ],
            ),
        ],
    }
}

fn test_batch_gen_schedulingpolicy_list_scheduling_policies(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "ListSchedulingPolicies",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_scheduling_policies()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.schedulingPolicies",
                vec![
                    scenario::where_entry("$.arn", scenario::context("schedulingpolicy.arn")),
                ],
            ),
        ],
    }
}

fn test_batch_gen_schedulingpolicy_delete_scheduling_policy(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteSchedulingPolicy",
            params: scenario::map(vec![
                ("arn", scenario::context("schedulingpolicy.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_scheduling_policy()
                            .arn(b.string("arn")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::absent_from_list(
                Some(Call {
                    op: "ListSchedulingPolicies",
                    params: scenario::lit(::serde_json::json!({})),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |_b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_scheduling_policies()
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.schedulingPolicies",
                vec![
                    scenario::where_entry("$.arn", scenario::context("schedulingpolicy.arn")),
                ],
            ),
        ],
    }
}

fn setup_batch_gen_consumableresource(_client: &aws_sdk_batch::Client) -> Vec<Call> {
    // An empty phase is a no-op, not a missing one.
    Vec::new()
}

fn teardown_batch_gen_consumableresource(client: &aws_sdk_batch::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteConsumableResource",
            params: scenario::map(vec![
                ("consumableResource", scenario::context("consumableresource.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_consumable_resource()
                            .consumable_resource(b.string("consumableResource")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_batch_gen_consumableresource_create_consumable_resource(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "CreateConsumableResource",
            params: scenario::map(vec![
                ("consumableResourceName", scenario::name("cr")),
                ("resourceType", scenario::lit(::serde_json::json!("REPLENISHABLE"))),
                ("totalQuantity", scenario::lit(::serde_json::json!(10))),
            ]),
            export: vec![
                ("consumableresource.arn", "$.consumableResourceArn"),
                ("consumableresource.name", "$.consumableResourceName"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_consumable_resource()
                            .consumable_resource_name(b.string("consumableResourceName")?)
                            .resource_type("REPLENISHABLE")
                            .total_quantity(10)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.consumableResourceArn"),
                scenario::non_empty("$.consumableResourceName"),
            ]),
            scenario::readback(
                Call {
                    op: "DescribeConsumableResource",
                    params: scenario::map(vec![
                        ("consumableResource", scenario::context("consumableresource.arn")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .describe_consumable_resource()
                                    .consumable_resource(b.string("consumableResource")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.consumableResourceArn", scenario::context("consumableresource.arn")),
                ],
            ),
            scenario::list_contains(
                Some(Call {
                    op: "ListConsumableResources",
                    params: scenario::lit(::serde_json::json!({})),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |_b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_consumable_resources()
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.consumableResources",
                vec![
                    scenario::where_entry("$.consumableResourceArn", scenario::context("consumableresource.arn")),
                ],
            ),
        ],
    }
}

fn test_batch_gen_consumableresource_describe_consumable_resource(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeConsumableResource",
            params: scenario::map(vec![
                ("consumableResource", scenario::context("consumableresource.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_consumable_resource()
                            .consumable_resource(b.string("consumableResource")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.consumableResourceArn", scenario::context("consumableresource.arn")),
            ]),
        ],
    }
}

fn test_batch_gen_consumableresource_update_consumable_resource(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "UpdateConsumableResource",
            params: scenario::map(vec![
                ("consumableResource", scenario::context("consumableresource.arn")),
                ("operation", scenario::lit(::serde_json::json!("SET"))),
                ("quantity", scenario::lit(::serde_json::json!(20))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .update_consumable_resource()
                            .consumable_resource(b.string("consumableResource")?)
                            .operation("SET")
                            .quantity(20)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::readback(
                Call {
                    op: "DescribeConsumableResource",
                    params: scenario::map(vec![
                        ("consumableResource", scenario::context("consumableresource.arn")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .describe_consumable_resource()
                                    .consumable_resource(b.string("consumableResource")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.totalQuantity", scenario::lit(::serde_json::json!(20))),
                ],
            ),
        ],
    }
}

fn test_batch_gen_consumableresource_tag_resource(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "TagResource",
            params: scenario::map(vec![
                ("resourceArn", scenario::context("consumableresource.arn")),
                ("tags", scenario::lit(::serde_json::json!({"compat": "scenario"}))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .tag_resource()
                            .resource_arn(b.string("resourceArn")?)
                            .tags("compat", "scenario")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::readback(
                Call {
                    op: "ListTagsForResource",
                    params: scenario::map(vec![
                        ("resourceArn", scenario::context("consumableresource.arn")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_tags_for_resource()
                                    .resource_arn(b.string("resourceArn")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.tags.compat", scenario::lit(::serde_json::json!("scenario"))),
                ],
            ),
        ],
    }
}

fn test_batch_gen_consumableresource_list_tags_for_resource(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "ListTagsForResource",
            params: scenario::map(vec![
                ("resourceArn", scenario::context("consumableresource.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_tags_for_resource()
                            .resource_arn(b.string("resourceArn")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.tags.compat", scenario::lit(::serde_json::json!("scenario"))),
            ]),
        ],
    }
}

fn test_batch_gen_consumableresource_untag_resource(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "UntagResource",
            params: scenario::map(vec![
                ("resourceArn", scenario::context("consumableresource.arn")),
                ("tagKeys", scenario::lit(::serde_json::json!(["compat"]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .untag_resource()
                            .resource_arn(b.string("resourceArn")?)
                            .tag_keys("compat")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::readback(
                Call {
                    op: "ListTagsForResource",
                    params: scenario::map(vec![
                        ("resourceArn", scenario::context("consumableresource.arn")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_tags_for_resource()
                                    .resource_arn(b.string("resourceArn")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::missing("$.tags.compat"),
                ],
            ),
        ],
    }
}

fn test_batch_gen_consumableresource_list_consumable_resources(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "ListConsumableResources",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_consumable_resources()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.consumableResources",
                vec![
                    scenario::where_entry("$.consumableResourceArn", scenario::context("consumableresource.arn")),
                ],
            ),
        ],
    }
}

fn test_batch_gen_consumableresource_delete_consumable_resource(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteConsumableResource",
            params: scenario::map(vec![
                ("consumableResource", scenario::context("consumableresource.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_consumable_resource()
                            .consumable_resource(b.string("consumableResource")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::absent_from_list(
                Some(Call {
                    op: "ListConsumableResources",
                    params: scenario::lit(::serde_json::json!({})),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |_b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_consumable_resources()
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.consumableResources",
                vec![
                    scenario::where_entry("$.consumableResourceArn", scenario::context("consumableresource.arn")),
                ],
            ),
        ],
    }
}

fn setup_batch_gen_computeenvironment(_client: &aws_sdk_batch::Client) -> Vec<Call> {
    // An empty phase is a no-op, not a missing one.
    Vec::new()
}

fn teardown_batch_gen_computeenvironment(client: &aws_sdk_batch::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteComputeEnvironment",
            params: scenario::map(vec![
                ("computeEnvironment", scenario::context("computeenvironment.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_compute_environment()
                            .compute_environment(b.string("computeEnvironment")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_batch_gen_computeenvironment_create_compute_environment(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "CreateComputeEnvironment",
            params: scenario::map(vec![
                ("computeEnvironmentName", scenario::name("ce")),
                ("state", scenario::lit(::serde_json::json!("ENABLED"))),
                ("type", scenario::lit(::serde_json::json!("UNMANAGED"))),
            ]),
            export: vec![
                ("computeenvironment.arn", "$.computeEnvironmentArn"),
                ("computeenvironment.name", "$.computeEnvironmentName"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_compute_environment()
                            .compute_environment_name(b.string("computeEnvironmentName")?)
                            .state(aws_sdk_batch::types::CeState::from("ENABLED"))
                            .r#type(aws_sdk_batch::types::CeType::from("UNMANAGED"))
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.computeEnvironmentArn"),
                scenario::non_empty("$.computeEnvironmentName"),
            ]),
            scenario::eventually(30, 2000,
                scenario::readback(
                    Call {
                        op: "DescribeComputeEnvironments",
                        params: scenario::map(vec![
                            ("computeEnvironments", scenario::list(vec![
                                scenario::context("computeenvironment.arn"),
                            ])),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .describe_compute_environments()
                                        .compute_environments(b.string("computeEnvironments[0]")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.computeEnvironments[0].computeEnvironmentArn", scenario::context("computeenvironment.arn")),
                        scenario::equals("$.computeEnvironments[0].state", scenario::lit(::serde_json::json!("ENABLED"))),
                    ],
                ),
            ),
            scenario::eventually(30, 2000,
                scenario::list_contains(
                    Some(Call {
                        op: "DescribeComputeEnvironments",
                        params: scenario::lit(::serde_json::json!({})),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |_b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .describe_compute_environments()
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.computeEnvironments",
                    vec![
                        scenario::where_entry("$.computeEnvironmentArn", scenario::context("computeenvironment.arn")),
                    ],
                ),
            ),
        ],
    }
}

fn test_batch_gen_computeenvironment_describe_compute_environments(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeComputeEnvironments",
            params: scenario::map(vec![
                ("computeEnvironments", scenario::list(vec![
                    scenario::context("computeenvironment.arn"),
                ])),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_compute_environments()
                            .compute_environments(b.string("computeEnvironments[0]")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.computeEnvironments[0].computeEnvironmentArn", scenario::context("computeenvironment.arn")),
            ]),
        ],
    }
}

fn test_batch_gen_computeenvironment_update_compute_environment(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "UpdateComputeEnvironment",
            params: scenario::map(vec![
                ("computeEnvironment", scenario::context("computeenvironment.arn")),
                ("state", scenario::lit(::serde_json::json!("DISABLED"))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .update_compute_environment()
                            .compute_environment(b.string("computeEnvironment")?)
                            .state(aws_sdk_batch::types::CeState::from("DISABLED"))
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(30, 2000,
                scenario::readback(
                    Call {
                        op: "DescribeComputeEnvironments",
                        params: scenario::map(vec![
                            ("computeEnvironments", scenario::list(vec![
                                scenario::context("computeenvironment.arn"),
                            ])),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .describe_compute_environments()
                                        .compute_environments(b.string("computeEnvironments[0]")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.computeEnvironments[0].state", scenario::lit(::serde_json::json!("DISABLED"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_batch_gen_computeenvironment_tag_resource(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "TagResource",
            params: scenario::map(vec![
                ("resourceArn", scenario::context("computeenvironment.arn")),
                ("tags", scenario::lit(::serde_json::json!({"compat": "scenario"}))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .tag_resource()
                            .resource_arn(b.string("resourceArn")?)
                            .tags("compat", "scenario")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(30, 2000,
                scenario::readback(
                    Call {
                        op: "ListTagsForResource",
                        params: scenario::map(vec![
                            ("resourceArn", scenario::context("computeenvironment.arn")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_tags_for_resource()
                                        .resource_arn(b.string("resourceArn")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.tags.compat", scenario::lit(::serde_json::json!("scenario"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_batch_gen_computeenvironment_list_tags_for_resource(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "ListTagsForResource",
            params: scenario::map(vec![
                ("resourceArn", scenario::context("computeenvironment.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_tags_for_resource()
                            .resource_arn(b.string("resourceArn")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.tags.compat", scenario::lit(::serde_json::json!("scenario"))),
            ]),
        ],
    }
}

fn test_batch_gen_computeenvironment_untag_resource(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "UntagResource",
            params: scenario::map(vec![
                ("resourceArn", scenario::context("computeenvironment.arn")),
                ("tagKeys", scenario::lit(::serde_json::json!(["compat"]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .untag_resource()
                            .resource_arn(b.string("resourceArn")?)
                            .tag_keys("compat")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(30, 2000,
                scenario::readback(
                    Call {
                        op: "ListTagsForResource",
                        params: scenario::map(vec![
                            ("resourceArn", scenario::context("computeenvironment.arn")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_tags_for_resource()
                                        .resource_arn(b.string("resourceArn")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::missing("$.tags.compat"),
                    ],
                ),
            ),
        ],
    }
}

fn test_batch_gen_computeenvironment_delete_compute_environment(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteComputeEnvironment",
            params: scenario::map(vec![
                ("computeEnvironment", scenario::context("computeenvironment.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_compute_environment()
                            .compute_environment(b.string("computeEnvironment")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(30, 2000,
                scenario::absent_from_list(
                    Some(Call {
                        op: "DescribeComputeEnvironments",
                        params: scenario::lit(::serde_json::json!({})),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |_b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .describe_compute_environments()
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.computeEnvironments",
                    vec![
                        scenario::where_entry("$.computeEnvironmentArn", scenario::context("computeenvironment.arn")),
                    ],
                ),
            ),
        ],
    }
}

fn setup_batch_gen_jobqueue(client: &aws_sdk_batch::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreateComputeEnvironment",
            params: scenario::map(vec![
                ("computeEnvironmentName", scenario::name("ce")),
                ("state", scenario::lit(::serde_json::json!("ENABLED"))),
                ("type", scenario::lit(::serde_json::json!("UNMANAGED"))),
            ]),
            export: vec![
                ("computeenvironment.arn", "$.computeEnvironmentArn"),
                ("computeenvironment.name", "$.computeEnvironmentName"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_compute_environment()
                            .compute_environment_name(b.string("computeEnvironmentName")?)
                            .state(aws_sdk_batch::types::CeState::from("ENABLED"))
                            .r#type(aws_sdk_batch::types::CeType::from("UNMANAGED"))
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_batch_gen_jobqueue(client: &aws_sdk_batch::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteJobQueue",
            params: scenario::map(vec![
                ("jobQueue", scenario::context("jobqueue.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_job_queue()
                            .job_queue(b.string("jobQueue")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteComputeEnvironment",
            params: scenario::map(vec![
                ("computeEnvironment", scenario::context("computeenvironment.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_compute_environment()
                            .compute_environment(b.string("computeEnvironment")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_batch_gen_jobqueue_create_job_queue(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "CreateJobQueue",
            params: scenario::map(vec![
                ("computeEnvironmentOrder", scenario::list(vec![
                    scenario::map(vec![
                        ("computeEnvironment", scenario::context("computeenvironment.arn")),
                        ("order", scenario::lit(::serde_json::json!(1))),
                    ]),
                ])),
                ("jobQueueName", scenario::name("jq")),
                ("priority", scenario::lit(::serde_json::json!(1))),
                ("state", scenario::lit(::serde_json::json!("ENABLED"))),
            ]),
            export: vec![
                ("jobqueue.arn", "$.jobQueueArn"),
                ("jobqueue.name", "$.jobQueueName"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_job_queue()
                            .compute_environment_order(
                                aws_sdk_batch::types::ComputeEnvironmentOrder::builder()
                                    .compute_environment(b.string("computeEnvironmentOrder[0].computeEnvironment")?)
                                    .order(1)
                                    .build()
                            )
                            .job_queue_name(b.string("jobQueueName")?)
                            .priority(1)
                            .state(aws_sdk_batch::types::JqState::from("ENABLED"))
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.jobQueueArn"),
                scenario::non_empty("$.jobQueueName"),
            ]),
            scenario::eventually(30, 2000,
                scenario::readback(
                    Call {
                        op: "DescribeJobQueues",
                        params: scenario::map(vec![
                            ("jobQueues", scenario::list(vec![
                                scenario::context("jobqueue.arn"),
                            ])),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .describe_job_queues()
                                        .job_queues(b.string("jobQueues[0]")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.jobQueues[0].jobQueueArn", scenario::context("jobqueue.arn")),
                        scenario::equals("$.jobQueues[0].state", scenario::lit(::serde_json::json!("ENABLED"))),
                    ],
                ),
            ),
            scenario::eventually(30, 2000,
                scenario::list_contains(
                    Some(Call {
                        op: "DescribeJobQueues",
                        params: scenario::lit(::serde_json::json!({})),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |_b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .describe_job_queues()
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.jobQueues",
                    vec![
                        scenario::where_entry("$.jobQueueArn", scenario::context("jobqueue.arn")),
                    ],
                ),
            ),
        ],
    }
}

fn test_batch_gen_jobqueue_describe_job_queues(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeJobQueues",
            params: scenario::map(vec![
                ("jobQueues", scenario::list(vec![scenario::context("jobqueue.arn")])),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_job_queues()
                            .job_queues(b.string("jobQueues[0]")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.jobQueues[0].jobQueueArn", scenario::context("jobqueue.arn")),
            ]),
        ],
    }
}

fn test_batch_gen_jobqueue_update_job_queue(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "UpdateJobQueue",
            params: scenario::map(vec![
                ("jobQueue", scenario::context("jobqueue.arn")),
                ("state", scenario::lit(::serde_json::json!("DISABLED"))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .update_job_queue()
                            .job_queue(b.string("jobQueue")?)
                            .state(aws_sdk_batch::types::JqState::from("DISABLED"))
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(30, 2000,
                scenario::readback(
                    Call {
                        op: "DescribeJobQueues",
                        params: scenario::map(vec![
                            ("jobQueues", scenario::list(vec![
                                scenario::context("jobqueue.arn"),
                            ])),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .describe_job_queues()
                                        .job_queues(b.string("jobQueues[0]")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.jobQueues[0].state", scenario::lit(::serde_json::json!("DISABLED"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_batch_gen_jobqueue_tag_resource(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "TagResource",
            params: scenario::map(vec![
                ("resourceArn", scenario::context("jobqueue.arn")),
                ("tags", scenario::lit(::serde_json::json!({"compat": "scenario"}))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .tag_resource()
                            .resource_arn(b.string("resourceArn")?)
                            .tags("compat", "scenario")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(30, 2000,
                scenario::readback(
                    Call {
                        op: "ListTagsForResource",
                        params: scenario::map(vec![
                            ("resourceArn", scenario::context("jobqueue.arn")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_tags_for_resource()
                                        .resource_arn(b.string("resourceArn")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.tags.compat", scenario::lit(::serde_json::json!("scenario"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_batch_gen_jobqueue_list_tags_for_resource(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "ListTagsForResource",
            params: scenario::map(vec![
                ("resourceArn", scenario::context("jobqueue.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_tags_for_resource()
                            .resource_arn(b.string("resourceArn")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.tags.compat", scenario::lit(::serde_json::json!("scenario"))),
            ]),
        ],
    }
}

fn test_batch_gen_jobqueue_untag_resource(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "UntagResource",
            params: scenario::map(vec![
                ("resourceArn", scenario::context("jobqueue.arn")),
                ("tagKeys", scenario::lit(::serde_json::json!(["compat"]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .untag_resource()
                            .resource_arn(b.string("resourceArn")?)
                            .tag_keys("compat")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(30, 2000,
                scenario::readback(
                    Call {
                        op: "ListTagsForResource",
                        params: scenario::map(vec![
                            ("resourceArn", scenario::context("jobqueue.arn")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_tags_for_resource()
                                        .resource_arn(b.string("resourceArn")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::missing("$.tags.compat"),
                    ],
                ),
            ),
        ],
    }
}

fn test_batch_gen_jobqueue_get_job_queue_snapshot(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "GetJobQueueSnapshot",
            params: scenario::map(vec![
                ("jobQueue", scenario::context("jobqueue.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_job_queue_snapshot()
                            .job_queue(b.string("jobQueue")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.frontOfQueue.jobs"),
            ]),
        ],
    }
}

fn test_batch_gen_jobqueue_list_jobs(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "ListJobs",
            params: scenario::map(vec![
                ("jobQueue", scenario::context("jobqueue.arn")),
                ("jobStatus", scenario::lit(::serde_json::json!("SUBMITTED"))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_jobs()
                            .job_queue(b.string("jobQueue")?)
                            .job_status(aws_sdk_batch::types::JobStatus::from("SUBMITTED"))
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.jobSummaryList"),
            ]),
        ],
    }
}

fn test_batch_gen_jobqueue_delete_job_queue(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteJobQueue",
            params: scenario::map(vec![
                ("jobQueue", scenario::context("jobqueue.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_job_queue()
                            .job_queue(b.string("jobQueue")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(30, 2000,
                scenario::absent_from_list(
                    Some(Call {
                        op: "DescribeJobQueues",
                        params: scenario::lit(::serde_json::json!({})),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |_b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .describe_job_queues()
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.jobQueues",
                    vec![
                        scenario::where_entry("$.jobQueueArn", scenario::context("jobqueue.arn")),
                    ],
                ),
            ),
        ],
    }
}

fn setup_batch_gen_probe(_client: &aws_sdk_batch::Client) -> Vec<Call> {
    // An empty phase is a no-op, not a missing one.
    Vec::new()
}

fn teardown_batch_gen_probe(_client: &aws_sdk_batch::Client) -> Vec<Call> {
    // An empty phase is a no-op, not a missing one.
    Vec::new()
}

fn test_batch_gen_probe_describe_jobs(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeJobs",
            params: scenario::lit(::serde_json::json!({"jobs": ["00000000-0000-0000-0000-000000000000"]})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_jobs()
                            .jobs("00000000-0000-0000-0000-000000000000")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.jobs"),
            ]),
        ],
    }
}

fn test_batch_gen_probe_describe_quota_share(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeQuotaShare",
            params: scenario::lit(::serde_json::json!({"quotaShareArn": "arn:aws:batch:us-east-1:123456789012:quota-share/compat-scenario-nonexistent"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_quota_share()
                            .quota_share_arn("arn:aws:batch:us-east-1:123456789012:quota-share/compat-scenario-nonexistent")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.jobQueueArn"),
            ]),
        ],
    }
}

fn test_batch_gen_probe_describe_service_environments(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeServiceEnvironments",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_service_environments()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.serviceEnvironments"),
            ]),
        ],
    }
}

fn test_batch_gen_probe_describe_service_job(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeServiceJob",
            params: scenario::lit(::serde_json::json!({"jobId": "00000000-0000-0000-0000-000000000000"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_service_job()
                            .job_id("00000000-0000-0000-0000-000000000000")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.jobArn"),
            ]),
        ],
    }
}

fn test_batch_gen_probe_list_jobs_by_consumable_resource(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "ListJobsByConsumableResource",
            params: scenario::lit(::serde_json::json!({"consumableResource": "compat-scenario-nonexistent"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_jobs_by_consumable_resource()
                            .consumable_resource("compat-scenario-nonexistent")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.jobs"),
            ]),
        ],
    }
}

fn test_batch_gen_probe_list_quota_shares(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "ListQuotaShares",
            params: scenario::lit(::serde_json::json!({"jobQueue": "compat-scenario-nonexistent"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_quota_shares()
                            .job_queue("compat-scenario-nonexistent")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.quotaShares"),
            ]),
        ],
    }
}

fn test_batch_gen_probe_list_service_jobs(client: &aws_sdk_batch::Client) -> Test {
    Test {
        call: Call {
            op: "ListServiceJobs",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_service_jobs()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.jobSummaryList"),
            ]),
        ],
    }
}
