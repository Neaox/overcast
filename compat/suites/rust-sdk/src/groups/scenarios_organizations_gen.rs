// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/scenarios/organizations.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/scenarios/organizations.json";

const GROUP_ORGANIZATIONS_GEN_POLICY: Group = Group {
    name: "organizations-gen-policy",
    file: SCENARIO_FILE,
};

const GROUP_ORGANIZATIONS_GEN_ORGANIZATION: Group = Group {
    name: "organizations-gen-organization",
    file: SCENARIO_FILE,
};

const GROUP_ORGANIZATIONS_GEN_ROOT: Group = Group {
    name: "organizations-gen-root",
    file: SCENARIO_FILE,
};

const GROUP_ORGANIZATIONS_GEN_OU: Group = Group {
    name: "organizations-gen-ou",
    file: SCENARIO_FILE,
};

const GROUP_ORGANIZATIONS_GEN_PROBE: Group = Group {
    name: "organizations-gen-probe",
    file: SCENARIO_FILE,
};

/// The generated organizations groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosOrganizations {
    client: aws_sdk_organizations::Client,
}

impl ScenariosOrganizations {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_organizations::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_organizations::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosOrganizations {
    fn name(&self) -> &'static str {
        "scenarios/organizations"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-policy:CreatePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_POLICY
                            .run_test(&ctx, "CreatePolicy", test_organizations_gen_policy_create_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-policy:DescribePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_POLICY
                            .run_test(&ctx, "DescribePolicy", test_organizations_gen_policy_describe_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-policy:UpdatePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_POLICY
                            .run_test(&ctx, "UpdatePolicy", test_organizations_gen_policy_update_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-policy:TagResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_POLICY
                            .run_test(&ctx, "TagResource", test_organizations_gen_policy_tag_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-policy:ListTagsForResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_POLICY
                            .run_test(&ctx, "ListTagsForResource", test_organizations_gen_policy_list_tags_for_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-policy:UntagResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_POLICY
                            .run_test(&ctx, "UntagResource", test_organizations_gen_policy_untag_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-policy:ListPolicies".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_POLICY
                            .run_test(&ctx, "ListPolicies", test_organizations_gen_policy_list_policies(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-policy:DeletePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_POLICY
                            .run_test(&ctx, "DeletePolicy", test_organizations_gen_policy_delete_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-organization:DescribeOrganization".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_ORGANIZATION
                            .run_test(&ctx, "DescribeOrganization", test_organizations_gen_organization_describe_organization(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-root:ListRoots".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_ROOT
                            .run_test(&ctx, "ListRoots", test_organizations_gen_root_list_roots(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-ou:CreateOrganizationalUnit".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_OU
                            .run_test(&ctx, "CreateOrganizationalUnit", test_organizations_gen_ou_create_organizational_unit(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-ou:DescribeOrganizationalUnit".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_OU
                            .run_test(&ctx, "DescribeOrganizationalUnit", test_organizations_gen_ou_describe_organizational_unit(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-ou:UpdateOrganizationalUnit".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_OU
                            .run_test(&ctx, "UpdateOrganizationalUnit", test_organizations_gen_ou_update_organizational_unit(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-ou:TagResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_OU
                            .run_test(&ctx, "TagResource", test_organizations_gen_ou_tag_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-ou:ListTagsForResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_OU
                            .run_test(&ctx, "ListTagsForResource", test_organizations_gen_ou_list_tags_for_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-ou:UntagResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_OU
                            .run_test(&ctx, "UntagResource", test_organizations_gen_ou_untag_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-ou:ListOrganizationalUnitsForParent".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_OU
                            .run_test(&ctx, "ListOrganizationalUnitsForParent", test_organizations_gen_ou_list_organizational_units_for_parent(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-ou:DeleteOrganizationalUnit".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_OU
                            .run_test(&ctx, "DeleteOrganizationalUnit", test_organizations_gen_ou_delete_organizational_unit(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-probe:DescribeAccount".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE
                            .run_test(&ctx, "DescribeAccount", test_organizations_gen_probe_describe_account(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-probe:DescribeCreateAccountStatus".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE
                            .run_test(&ctx, "DescribeCreateAccountStatus", test_organizations_gen_probe_describe_create_account_status(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-probe:DescribeEffectivePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE
                            .run_test(&ctx, "DescribeEffectivePolicy", test_organizations_gen_probe_describe_effective_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-probe:DescribeHandshake".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE
                            .run_test(&ctx, "DescribeHandshake", test_organizations_gen_probe_describe_handshake(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-probe:DescribeResourcePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE
                            .run_test(&ctx, "DescribeResourcePolicy", test_organizations_gen_probe_describe_resource_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-probe:DescribeResponsibilityTransfer".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE
                            .run_test(&ctx, "DescribeResponsibilityTransfer", test_organizations_gen_probe_describe_responsibility_transfer(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-probe:ListAWSServiceAccessForOrganization".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE
                            .run_test(&ctx, "ListAWSServiceAccessForOrganization", test_organizations_gen_probe_list_aws_service_access_for_organization(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-probe:ListAccounts".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE
                            .run_test(&ctx, "ListAccounts", test_organizations_gen_probe_list_accounts(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-probe:ListAccountsForParent".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE
                            .run_test(&ctx, "ListAccountsForParent", test_organizations_gen_probe_list_accounts_for_parent(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-probe:ListAccountsWithInvalidEffectivePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE
                            .run_test(&ctx, "ListAccountsWithInvalidEffectivePolicy", test_organizations_gen_probe_list_accounts_with_invalid_effective_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-probe:ListChildren".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE
                            .run_test(&ctx, "ListChildren", test_organizations_gen_probe_list_children(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-probe:ListCreateAccountStatus".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE
                            .run_test(&ctx, "ListCreateAccountStatus", test_organizations_gen_probe_list_create_account_status(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-probe:ListDelegatedAdministrators".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE
                            .run_test(&ctx, "ListDelegatedAdministrators", test_organizations_gen_probe_list_delegated_administrators(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-probe:ListDelegatedServicesForAccount".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE
                            .run_test(&ctx, "ListDelegatedServicesForAccount", test_organizations_gen_probe_list_delegated_services_for_account(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-probe:ListEffectivePolicyValidationErrors".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE
                            .run_test(&ctx, "ListEffectivePolicyValidationErrors", test_organizations_gen_probe_list_effective_policy_validation_errors(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-probe:ListHandshakesForAccount".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE
                            .run_test(&ctx, "ListHandshakesForAccount", test_organizations_gen_probe_list_handshakes_for_account(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-probe:ListHandshakesForOrganization".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE
                            .run_test(&ctx, "ListHandshakesForOrganization", test_organizations_gen_probe_list_handshakes_for_organization(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-probe:ListInboundResponsibilityTransfers".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE
                            .run_test(&ctx, "ListInboundResponsibilityTransfers", test_organizations_gen_probe_list_inbound_responsibility_transfers(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-probe:ListOutboundResponsibilityTransfers".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE
                            .run_test(&ctx, "ListOutboundResponsibilityTransfers", test_organizations_gen_probe_list_outbound_responsibility_transfers(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-probe:ListParents".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE
                            .run_test(&ctx, "ListParents", test_organizations_gen_probe_list_parents(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-probe:ListPoliciesForTarget".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE
                            .run_test(&ctx, "ListPoliciesForTarget", test_organizations_gen_probe_list_policies_for_target(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "organizations-gen-probe:ListTargetsForPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE
                            .run_test(&ctx, "ListTargetsForPolicy", test_organizations_gen_probe_list_targets_for_policy(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "organizations-gen-policy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_POLICY.run_setup(&ctx, setup_organizations_gen_policy(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            setups.insert(
                "organizations-gen-organization".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_ORGANIZATION.run_setup(&ctx, setup_organizations_gen_organization(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            setups.insert(
                "organizations-gen-root".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_ROOT.run_setup(&ctx, setup_organizations_gen_root(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            setups.insert(
                "organizations-gen-ou".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_OU.run_setup(&ctx, setup_organizations_gen_ou(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            setups.insert(
                "organizations-gen-probe".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE.run_setup(&ctx, setup_organizations_gen_probe(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "organizations-gen-policy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_POLICY.run_teardown(&ctx, teardown_organizations_gen_policy(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            teardowns.insert(
                "organizations-gen-organization".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_ORGANIZATION.run_teardown(&ctx, teardown_organizations_gen_organization(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            teardowns.insert(
                "organizations-gen-root".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_ROOT.run_teardown(&ctx, teardown_organizations_gen_root(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            teardowns.insert(
                "organizations-gen-ou".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_OU.run_teardown(&ctx, teardown_organizations_gen_ou(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            teardowns.insert(
                "organizations-gen-probe".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ORGANIZATIONS_GEN_PROBE.run_teardown(&ctx, teardown_organizations_gen_probe(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_organizations_gen_policy(_client: &aws_sdk_organizations::Client) -> Vec<Call> {
    // An empty phase is a no-op, not a missing one.
    Vec::new()
}

fn teardown_organizations_gen_policy(client: &aws_sdk_organizations::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeletePolicy",
            params: scenario::map(vec![
                ("PolicyId", scenario::context("policy.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_policy()
                            .policy_id(b.string("PolicyId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_organizations_gen_policy_create_policy(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "CreatePolicy",
            params: scenario::map(vec![
                ("Content", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"*\",\"Resource\":\"*\"}]}"))),
                ("Description", scenario::lit(::serde_json::json!("compat scenario policy"))),
                ("Name", scenario::name("policy")),
                ("Type", scenario::lit(::serde_json::json!("SERVICE_CONTROL_POLICY"))),
            ]),
            export: vec![
                ("policy.arn", "$.Policy.PolicySummary.Arn"),
                ("policy.id", "$.Policy.PolicySummary.Id"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_policy()
                            .content("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"*\",\"Resource\":\"*\"}]}")
                            .description("compat scenario policy")
                            .name(b.string("Name")?)
                            .r#type(aws_sdk_organizations::types::PolicyType::from("SERVICE_CONTROL_POLICY"))
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::matches("$.Policy.PolicySummary.Arn", "^(arn:aws:organizations::\\d{12}:policy\\/o-[a-z0-9]{10,32}\\/[0-9a-z_]+\\/p-[0-9a-z]{10,32})|(arn:aws:organizations::aws:policy\\/[0-9a-z_]+\\/p-[0-9a-zA-Z_]{10,128})$"),
                scenario::matches("$.Policy.PolicySummary.Id", "^p-[0-9a-zA-Z_]{8,128}$"),
            ]),
            scenario::readback(
                Call {
                    op: "DescribePolicy",
                    params: scenario::map(vec![
                        ("PolicyId", scenario::context("policy.id")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .describe_policy()
                                    .policy_id(b.string("PolicyId")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.Policy.PolicySummary.Description", scenario::lit(::serde_json::json!("compat scenario policy"))),
                    scenario::equals("$.Policy.PolicySummary.Id", scenario::context("policy.id")),
                ],
            ),
            scenario::list_contains(
                Some(Call {
                    op: "ListPolicies",
                    params: scenario::lit(::serde_json::json!({"Filter": "SERVICE_CONTROL_POLICY"})),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |_b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_policies()
                                    .filter(aws_sdk_organizations::types::PolicyType::from("SERVICE_CONTROL_POLICY"))
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.Policies",
                vec![
                    scenario::where_entry("$.Id", scenario::context("policy.id")),
                ],
            ),
        ],
    }
}

fn test_organizations_gen_policy_describe_policy(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "DescribePolicy",
            params: scenario::map(vec![
                ("PolicyId", scenario::context("policy.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_policy()
                            .policy_id(b.string("PolicyId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.Policy.PolicySummary.Id", scenario::context("policy.id")),
            ]),
        ],
    }
}

fn test_organizations_gen_policy_update_policy(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "UpdatePolicy",
            params: scenario::map(vec![
                ("Description", scenario::lit(::serde_json::json!("compat scenario policy, updated"))),
                ("PolicyId", scenario::context("policy.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .update_policy()
                            .description("compat scenario policy, updated")
                            .policy_id(b.string("PolicyId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::readback(
                Call {
                    op: "DescribePolicy",
                    params: scenario::map(vec![
                        ("PolicyId", scenario::context("policy.id")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .describe_policy()
                                    .policy_id(b.string("PolicyId")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.Policy.PolicySummary.Description", scenario::lit(::serde_json::json!("compat scenario policy, updated"))),
                ],
            ),
        ],
    }
}

fn test_organizations_gen_policy_tag_resource(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "TagResource",
            params: scenario::map(vec![
                ("ResourceId", scenario::context("policy.id")),
                ("Tags", scenario::lit(::serde_json::json!([{"Key": "compat", "Value": "scenario"}]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .tag_resource()
                            .resource_id(b.string("ResourceId")?)
                            .tags(
                                aws_sdk_organizations::types::Tag::builder()
                                    .key("compat")
                                    .value("scenario")
                                    .build()
                                    .map_err(|err| scenario::build_error("Tags[0]", err))?
                            )
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                Some(Call {
                    op: "ListTagsForResource",
                    params: scenario::map(vec![
                        ("ResourceId", scenario::context("policy.id")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_tags_for_resource()
                                    .resource_id(b.string("ResourceId")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.Tags",
                vec![
                    scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("compat"))),
                    scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("scenario"))),
                ],
            ),
        ],
    }
}

fn test_organizations_gen_policy_list_tags_for_resource(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "ListTagsForResource",
            params: scenario::map(vec![
                ("ResourceId", scenario::context("policy.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_tags_for_resource()
                            .resource_id(b.string("ResourceId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Tags",
                vec![
                    scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("compat"))),
                    scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("scenario"))),
                ],
            ),
        ],
    }
}

fn test_organizations_gen_policy_untag_resource(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "UntagResource",
            params: scenario::map(vec![
                ("ResourceId", scenario::context("policy.id")),
                ("TagKeys", scenario::lit(::serde_json::json!(["compat"]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .untag_resource()
                            .resource_id(b.string("ResourceId")?)
                            .tag_keys("compat")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::absent_from_list(
                Some(Call {
                    op: "ListTagsForResource",
                    params: scenario::map(vec![
                        ("ResourceId", scenario::context("policy.id")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_tags_for_resource()
                                    .resource_id(b.string("ResourceId")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.Tags",
                vec![
                    scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("compat"))),
                ],
            ),
        ],
    }
}

fn test_organizations_gen_policy_list_policies(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "ListPolicies",
            params: scenario::lit(::serde_json::json!({"Filter": "SERVICE_CONTROL_POLICY"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_policies()
                            .filter(aws_sdk_organizations::types::PolicyType::from("SERVICE_CONTROL_POLICY"))
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Policies",
                vec![
                    scenario::where_entry("$.Id", scenario::context("policy.id")),
                ],
            ),
        ],
    }
}

fn test_organizations_gen_policy_delete_policy(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "DeletePolicy",
            params: scenario::map(vec![
                ("PolicyId", scenario::context("policy.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_policy()
                            .policy_id(b.string("PolicyId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::absent_by_error(
                Call {
                    op: "DescribePolicy",
                    params: scenario::map(vec![
                        ("PolicyId", scenario::context("policy.id")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .describe_policy()
                                    .policy_id(b.string("PolicyId")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                scenario::error("PolicyNotFoundException", "PolicyNotFoundException"),
            ),
        ],
    }
}

fn setup_organizations_gen_organization(_client: &aws_sdk_organizations::Client) -> Vec<Call> {
    // An empty phase is a no-op, not a missing one.
    Vec::new()
}

fn teardown_organizations_gen_organization(_client: &aws_sdk_organizations::Client) -> Vec<Call> {
    // An empty phase is a no-op, not a missing one.
    Vec::new()
}

fn test_organizations_gen_organization_describe_organization(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeOrganization",
            params: scenario::lit(::serde_json::json!({})),
            export: vec![
                ("organization.arn", "$.Organization.Arn"),
                ("organization.id", "$.Organization.Id"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_organization()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::matches("$.Organization.Arn", "^arn:aws:organizations::\\d{12}:organization\\/o-[a-z0-9]{10,32}$"),
            ]),
        ],
    }
}

fn setup_organizations_gen_root(_client: &aws_sdk_organizations::Client) -> Vec<Call> {
    // An empty phase is a no-op, not a missing one.
    Vec::new()
}

fn teardown_organizations_gen_root(_client: &aws_sdk_organizations::Client) -> Vec<Call> {
    // An empty phase is a no-op, not a missing one.
    Vec::new()
}

fn test_organizations_gen_root_list_roots(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "ListRoots",
            params: scenario::lit(::serde_json::json!({})),
            export: vec![
                ("root.id", "$.Roots[0].Id"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_roots()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::matches("$.Roots[0].Id", "^r-[0-9a-z]{4,32}$"),
            ]),
        ],
    }
}

fn setup_organizations_gen_ou(client: &aws_sdk_organizations::Client) -> Vec<Call> {
    vec![
        Call {
            op: "ListRoots",
            params: scenario::lit(::serde_json::json!({})),
            export: vec![
                ("root.id", "$.Roots[0].Id"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_roots()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_organizations_gen_ou(client: &aws_sdk_organizations::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteOrganizationalUnit",
            params: scenario::map(vec![
                ("OrganizationalUnitId", scenario::context("ou.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_organizational_unit()
                            .organizational_unit_id(b.string("OrganizationalUnitId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_organizations_gen_ou_create_organizational_unit(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "CreateOrganizationalUnit",
            params: scenario::map(vec![
                ("Name", scenario::name("ou")),
                ("ParentId", scenario::context("root.id")),
            ]),
            export: vec![
                ("ou.arn", "$.OrganizationalUnit.Arn"),
                ("ou.id", "$.OrganizationalUnit.Id"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_organizational_unit()
                            .name(b.string("Name")?)
                            .parent_id(b.string("ParentId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::matches("$.OrganizationalUnit.Arn", "^arn:aws:organizations::\\d{12}:ou\\/o-[a-z0-9]{10,32}\\/ou-[0-9a-z]{4,32}-[0-9a-z]{8,32}$"),
                scenario::matches("$.OrganizationalUnit.Id", "^ou-[0-9a-z]{4,32}-[a-z0-9]{8,32}$"),
            ]),
            scenario::readback(
                Call {
                    op: "DescribeOrganizationalUnit",
                    params: scenario::map(vec![
                        ("OrganizationalUnitId", scenario::context("ou.id")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .describe_organizational_unit()
                                    .organizational_unit_id(b.string("OrganizationalUnitId")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.OrganizationalUnit.Id", scenario::context("ou.id")),
                    scenario::equals("$.OrganizationalUnit.Name", scenario::name("ou")),
                ],
            ),
            scenario::list_contains(
                Some(Call {
                    op: "ListOrganizationalUnitsForParent",
                    params: scenario::map(vec![
                        ("ParentId", scenario::context("root.id")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_organizational_units_for_parent()
                                    .parent_id(b.string("ParentId")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.OrganizationalUnits",
                vec![
                    scenario::where_entry("$.Id", scenario::context("ou.id")),
                ],
            ),
        ],
    }
}

fn test_organizations_gen_ou_describe_organizational_unit(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeOrganizationalUnit",
            params: scenario::map(vec![
                ("OrganizationalUnitId", scenario::context("ou.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_organizational_unit()
                            .organizational_unit_id(b.string("OrganizationalUnitId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.OrganizationalUnit.Id", scenario::context("ou.id")),
            ]),
        ],
    }
}

fn test_organizations_gen_ou_update_organizational_unit(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "UpdateOrganizationalUnit",
            params: scenario::map(vec![
                ("Name", scenario::name("ou-renamed")),
                ("OrganizationalUnitId", scenario::context("ou.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .update_organizational_unit()
                            .name(b.string("Name")?)
                            .organizational_unit_id(b.string("OrganizationalUnitId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::readback(
                Call {
                    op: "DescribeOrganizationalUnit",
                    params: scenario::map(vec![
                        ("OrganizationalUnitId", scenario::context("ou.id")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .describe_organizational_unit()
                                    .organizational_unit_id(b.string("OrganizationalUnitId")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.OrganizationalUnit.Name", scenario::name("ou-renamed")),
                ],
            ),
        ],
    }
}

fn test_organizations_gen_ou_tag_resource(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "TagResource",
            params: scenario::map(vec![
                ("ResourceId", scenario::context("ou.id")),
                ("Tags", scenario::lit(::serde_json::json!([{"Key": "compat", "Value": "scenario"}]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .tag_resource()
                            .resource_id(b.string("ResourceId")?)
                            .tags(
                                aws_sdk_organizations::types::Tag::builder()
                                    .key("compat")
                                    .value("scenario")
                                    .build()
                                    .map_err(|err| scenario::build_error("Tags[0]", err))?
                            )
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                Some(Call {
                    op: "ListTagsForResource",
                    params: scenario::map(vec![
                        ("ResourceId", scenario::context("ou.id")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_tags_for_resource()
                                    .resource_id(b.string("ResourceId")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.Tags",
                vec![
                    scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("compat"))),
                    scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("scenario"))),
                ],
            ),
        ],
    }
}

fn test_organizations_gen_ou_list_tags_for_resource(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "ListTagsForResource",
            params: scenario::map(vec![("ResourceId", scenario::context("ou.id"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_tags_for_resource()
                            .resource_id(b.string("ResourceId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Tags",
                vec![
                    scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("compat"))),
                    scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("scenario"))),
                ],
            ),
        ],
    }
}

fn test_organizations_gen_ou_untag_resource(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "UntagResource",
            params: scenario::map(vec![
                ("ResourceId", scenario::context("ou.id")),
                ("TagKeys", scenario::lit(::serde_json::json!(["compat"]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .untag_resource()
                            .resource_id(b.string("ResourceId")?)
                            .tag_keys("compat")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::absent_from_list(
                Some(Call {
                    op: "ListTagsForResource",
                    params: scenario::map(vec![
                        ("ResourceId", scenario::context("ou.id")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_tags_for_resource()
                                    .resource_id(b.string("ResourceId")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.Tags",
                vec![
                    scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("compat"))),
                ],
            ),
        ],
    }
}

fn test_organizations_gen_ou_list_organizational_units_for_parent(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "ListOrganizationalUnitsForParent",
            params: scenario::map(vec![("ParentId", scenario::context("root.id"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_organizational_units_for_parent()
                            .parent_id(b.string("ParentId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.OrganizationalUnits",
                vec![
                    scenario::where_entry("$.Id", scenario::context("ou.id")),
                ],
            ),
        ],
    }
}

fn test_organizations_gen_ou_delete_organizational_unit(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteOrganizationalUnit",
            params: scenario::map(vec![
                ("OrganizationalUnitId", scenario::context("ou.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_organizational_unit()
                            .organizational_unit_id(b.string("OrganizationalUnitId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::absent_by_error(
                Call {
                    op: "DescribeOrganizationalUnit",
                    params: scenario::map(vec![
                        ("OrganizationalUnitId", scenario::context("ou.id")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .describe_organizational_unit()
                                    .organizational_unit_id(b.string("OrganizationalUnitId")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                scenario::error("OrganizationalUnitNotFoundException", "OrganizationalUnitNotFoundException"),
            ),
        ],
    }
}

fn setup_organizations_gen_probe(_client: &aws_sdk_organizations::Client) -> Vec<Call> {
    // An empty phase is a no-op, not a missing one.
    Vec::new()
}

fn teardown_organizations_gen_probe(_client: &aws_sdk_organizations::Client) -> Vec<Call> {
    // An empty phase is a no-op, not a missing one.
    Vec::new()
}

fn test_organizations_gen_probe_describe_account(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeAccount",
            params: scenario::lit(::serde_json::json!({"AccountId": "123456789012"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_account()
                            .account_id("123456789012")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.Account"),
            ]),
        ],
    }
}

fn test_organizations_gen_probe_describe_create_account_status(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeCreateAccountStatus",
            params: scenario::lit(::serde_json::json!({"CreateAccountRequestId": "car-abcdefgh12345678"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_create_account_status()
                            .create_account_request_id("car-abcdefgh12345678")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.CreateAccountStatus"),
            ]),
        ],
    }
}

fn test_organizations_gen_probe_describe_effective_policy(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeEffectivePolicy",
            params: scenario::lit(::serde_json::json!({"PolicyType": "AISERVICES_OPT_OUT_POLICY"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_effective_policy()
                            .policy_type(aws_sdk_organizations::types::EffectivePolicyType::from("AISERVICES_OPT_OUT_POLICY"))
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.EffectivePolicy"),
            ]),
        ],
    }
}

fn test_organizations_gen_probe_describe_handshake(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeHandshake",
            params: scenario::lit(::serde_json::json!({"HandshakeId": "h-abcdefgh12345678"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_handshake()
                            .handshake_id("h-abcdefgh12345678")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.Handshake"),
            ]),
        ],
    }
}

fn test_organizations_gen_probe_describe_resource_policy(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeResourcePolicy",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_resource_policy()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.ResourcePolicy"),
            ]),
        ],
    }
}

fn test_organizations_gen_probe_describe_responsibility_transfer(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeResponsibilityTransfer",
            params: scenario::lit(::serde_json::json!({"Id": "rt-abcdefgh12345678"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_responsibility_transfer()
                            .id("rt-abcdefgh12345678")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.ResponsibilityTransfer"),
            ]),
        ],
    }
}

fn test_organizations_gen_probe_list_aws_service_access_for_organization(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "ListAWSServiceAccessForOrganization",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_aws_service_access_for_organization()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.EnabledServicePrincipals"),
            ]),
        ],
    }
}

fn test_organizations_gen_probe_list_accounts(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "ListAccounts",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_accounts()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.Accounts"),
            ]),
        ],
    }
}

fn test_organizations_gen_probe_list_accounts_for_parent(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "ListAccountsForParent",
            params: scenario::lit(::serde_json::json!({"ParentId": "r-abcd"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_accounts_for_parent()
                            .parent_id("r-abcd")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.Accounts"),
            ]),
        ],
    }
}

fn test_organizations_gen_probe_list_accounts_with_invalid_effective_policy(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "ListAccountsWithInvalidEffectivePolicy",
            params: scenario::lit(::serde_json::json!({"PolicyType": "AISERVICES_OPT_OUT_POLICY"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_accounts_with_invalid_effective_policy()
                            .policy_type(aws_sdk_organizations::types::EffectivePolicyType::from("AISERVICES_OPT_OUT_POLICY"))
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.PolicyType"),
            ]),
        ],
    }
}

fn test_organizations_gen_probe_list_children(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "ListChildren",
            params: scenario::lit(::serde_json::json!({"ChildType": "ACCOUNT", "ParentId": "r-abcd"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_children()
                            .child_type(aws_sdk_organizations::types::ChildType::from("ACCOUNT"))
                            .parent_id("r-abcd")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.Children"),
            ]),
        ],
    }
}

fn test_organizations_gen_probe_list_create_account_status(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "ListCreateAccountStatus",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_create_account_status()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.CreateAccountStatuses"),
            ]),
        ],
    }
}

fn test_organizations_gen_probe_list_delegated_administrators(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "ListDelegatedAdministrators",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_delegated_administrators()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.DelegatedAdministrators"),
            ]),
        ],
    }
}

fn test_organizations_gen_probe_list_delegated_services_for_account(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "ListDelegatedServicesForAccount",
            params: scenario::lit(::serde_json::json!({"AccountId": "123456789012"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_delegated_services_for_account()
                            .account_id("123456789012")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.DelegatedServices"),
            ]),
        ],
    }
}

fn test_organizations_gen_probe_list_effective_policy_validation_errors(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "ListEffectivePolicyValidationErrors",
            params: scenario::lit(::serde_json::json!({"AccountId": "123456789012", "PolicyType": "AISERVICES_OPT_OUT_POLICY"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_effective_policy_validation_errors()
                            .account_id("123456789012")
                            .policy_type(aws_sdk_organizations::types::EffectivePolicyType::from("AISERVICES_OPT_OUT_POLICY"))
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.AccountId"),
            ]),
        ],
    }
}

fn test_organizations_gen_probe_list_handshakes_for_account(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "ListHandshakesForAccount",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_handshakes_for_account()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.Handshakes"),
            ]),
        ],
    }
}

fn test_organizations_gen_probe_list_handshakes_for_organization(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "ListHandshakesForOrganization",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_handshakes_for_organization()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.Handshakes"),
            ]),
        ],
    }
}

fn test_organizations_gen_probe_list_inbound_responsibility_transfers(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "ListInboundResponsibilityTransfers",
            params: scenario::lit(::serde_json::json!({"Type": "BILLING"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_inbound_responsibility_transfers()
                            .r#type(aws_sdk_organizations::types::ResponsibilityTransferType::from("BILLING"))
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.ResponsibilityTransfers"),
            ]),
        ],
    }
}

fn test_organizations_gen_probe_list_outbound_responsibility_transfers(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "ListOutboundResponsibilityTransfers",
            params: scenario::lit(::serde_json::json!({"Type": "BILLING"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_outbound_responsibility_transfers()
                            .r#type(aws_sdk_organizations::types::ResponsibilityTransferType::from("BILLING"))
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.ResponsibilityTransfers"),
            ]),
        ],
    }
}

fn test_organizations_gen_probe_list_parents(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "ListParents",
            params: scenario::lit(::serde_json::json!({"ChildId": "ou-abcd-12345678"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_parents()
                            .child_id("ou-abcd-12345678")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.Parents"),
            ]),
        ],
    }
}

fn test_organizations_gen_probe_list_policies_for_target(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "ListPoliciesForTarget",
            params: scenario::lit(::serde_json::json!({"Filter": "AISERVICES_OPT_OUT_POLICY", "TargetId": "r-abcd"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_policies_for_target()
                            .filter(aws_sdk_organizations::types::PolicyType::from("AISERVICES_OPT_OUT_POLICY"))
                            .target_id("r-abcd")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.Policies"),
            ]),
        ],
    }
}

fn test_organizations_gen_probe_list_targets_for_policy(client: &aws_sdk_organizations::Client) -> Test {
    Test {
        call: Call {
            op: "ListTargetsForPolicy",
            params: scenario::lit(::serde_json::json!({"PolicyId": "p-abcd1234"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_targets_for_policy()
                            .policy_id("p-abcd1234")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.Targets"),
            ]),
        ],
    }
}
