// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/scenarios/secretsmanager.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/scenarios/secretsmanager.json";

const GROUP_SECRETSMANAGER_GEN_SECRET: Group = Group {
    name: "secretsmanager-gen-secret",
    file: SCENARIO_FILE,
};

const GROUP_SECRETSMANAGER_GEN_VERSION: Group = Group {
    name: "secretsmanager-gen-version",
    file: SCENARIO_FILE,
};

/// The generated secretsmanager groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosSecretsmanager {
    client: aws_sdk_secretsmanager::Client,
}

impl ScenariosSecretsmanager {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_secretsmanager::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_secretsmanager::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosSecretsmanager {
    fn name(&self) -> &'static str {
        "scenarios/secretsmanager"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "secretsmanager-gen-secret:CreateSecret".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SECRETSMANAGER_GEN_SECRET
                            .run_test(&ctx, "CreateSecret", test_secretsmanager_gen_secret_create_secret(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "secretsmanager-gen-secret:DescribeSecret".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SECRETSMANAGER_GEN_SECRET
                            .run_test(&ctx, "DescribeSecret", test_secretsmanager_gen_secret_describe_secret(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "secretsmanager-gen-secret:UpdateSecret".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SECRETSMANAGER_GEN_SECRET
                            .run_test(&ctx, "UpdateSecret", test_secretsmanager_gen_secret_update_secret(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "secretsmanager-gen-secret:TagResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SECRETSMANAGER_GEN_SECRET
                            .run_test(&ctx, "TagResource", test_secretsmanager_gen_secret_tag_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "secretsmanager-gen-secret:UntagResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SECRETSMANAGER_GEN_SECRET
                            .run_test(&ctx, "UntagResource", test_secretsmanager_gen_secret_untag_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "secretsmanager-gen-secret:GetRandomPassword".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SECRETSMANAGER_GEN_SECRET
                            .run_test(&ctx, "GetRandomPassword", test_secretsmanager_gen_secret_get_random_password(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "secretsmanager-gen-secret:BatchGetSecretValue".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SECRETSMANAGER_GEN_SECRET
                            .run_test(&ctx, "BatchGetSecretValue", test_secretsmanager_gen_secret_batch_get_secret_value(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "secretsmanager-gen-secret:ValidateResourcePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SECRETSMANAGER_GEN_SECRET
                            .run_test(&ctx, "ValidateResourcePolicy", test_secretsmanager_gen_secret_validate_resource_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "secretsmanager-gen-secret:PutResourcePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SECRETSMANAGER_GEN_SECRET
                            .run_test(&ctx, "PutResourcePolicy", test_secretsmanager_gen_secret_put_resource_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "secretsmanager-gen-secret:GetResourcePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SECRETSMANAGER_GEN_SECRET
                            .run_test(&ctx, "GetResourcePolicy", test_secretsmanager_gen_secret_get_resource_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "secretsmanager-gen-secret:DeleteResourcePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SECRETSMANAGER_GEN_SECRET
                            .run_test(&ctx, "DeleteResourcePolicy", test_secretsmanager_gen_secret_delete_resource_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "secretsmanager-gen-secret:CancelRotateSecret".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SECRETSMANAGER_GEN_SECRET
                            .run_test(&ctx, "CancelRotateSecret", test_secretsmanager_gen_secret_cancel_rotate_secret(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "secretsmanager-gen-secret:ListSecrets".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SECRETSMANAGER_GEN_SECRET
                            .run_test(&ctx, "ListSecrets", test_secretsmanager_gen_secret_list_secrets(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "secretsmanager-gen-secret:DeleteSecret".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SECRETSMANAGER_GEN_SECRET
                            .run_test(&ctx, "DeleteSecret", test_secretsmanager_gen_secret_delete_secret(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "secretsmanager-gen-version:PutSecretValue".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SECRETSMANAGER_GEN_VERSION
                            .run_test(&ctx, "PutSecretValue", test_secretsmanager_gen_version_put_secret_value(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "secretsmanager-gen-version:GetSecretValue".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SECRETSMANAGER_GEN_VERSION
                            .run_test(&ctx, "GetSecretValue", test_secretsmanager_gen_version_get_secret_value(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "secretsmanager-gen-version:UpdateSecretVersionStage".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SECRETSMANAGER_GEN_VERSION
                            .run_test(&ctx, "UpdateSecretVersionStage", test_secretsmanager_gen_version_update_secret_version_stage(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "secretsmanager-gen-version:ListSecretVersionIds".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SECRETSMANAGER_GEN_VERSION
                            .run_test(&ctx, "ListSecretVersionIds", test_secretsmanager_gen_version_list_secret_version_ids(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "secretsmanager-gen-secret".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SECRETSMANAGER_GEN_SECRET.run_setup(&ctx, setup_secretsmanager_gen_secret(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            setups.insert(
                "secretsmanager-gen-version".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SECRETSMANAGER_GEN_VERSION.run_setup(&ctx, setup_secretsmanager_gen_version(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "secretsmanager-gen-secret".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SECRETSMANAGER_GEN_SECRET.run_teardown(&ctx, teardown_secretsmanager_gen_secret(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            teardowns.insert(
                "secretsmanager-gen-version".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_SECRETSMANAGER_GEN_VERSION.run_teardown(&ctx, teardown_secretsmanager_gen_version(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_secretsmanager_gen_secret(_client: &aws_sdk_secretsmanager::Client) -> Vec<Call> {
    // An empty phase is a no-op, not a missing one.
    Vec::new()
}

fn teardown_secretsmanager_gen_secret(client: &aws_sdk_secretsmanager::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteSecret",
            params: scenario::map(vec![
                ("ForceDeleteWithoutRecovery", scenario::lit(::serde_json::json!(true))),
                ("SecretId", scenario::context("secret.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_secret()
                            .force_delete_without_recovery(true)
                            .secret_id(b.string("SecretId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_secretsmanager_gen_secret_create_secret(client: &aws_sdk_secretsmanager::Client) -> Test {
    Test {
        call: Call {
            op: "CreateSecret",
            params: scenario::map(vec![
                ("Description", scenario::lit(::serde_json::json!("compat scenario secret"))),
                ("Name", scenario::name("s")),
                ("SecretString", scenario::lit(::serde_json::json!("compat-scenario-initial"))),
            ]),
            export: vec![
                ("secret.arn", "$.ARN"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_secret()
                            .description("compat scenario secret")
                            .name(b.string("Name")?)
                            .secret_string("compat-scenario-initial")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.ARN"),
            ]),
            scenario::eventually(10, 1000,
                scenario::readback(
                    Call {
                        op: "DescribeSecret",
                        params: scenario::map(vec![
                            ("SecretId", scenario::context("secret.arn")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .describe_secret()
                                        .secret_id(b.string("SecretId")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.ARN", scenario::context("secret.arn")),
                        scenario::equals("$.Description", scenario::lit(::serde_json::json!("compat scenario secret"))),
                    ],
                ),
            ),
            scenario::eventually(10, 1000,
                scenario::list_contains(
                    Some(Call {
                        op: "ListSecrets",
                        params: scenario::map(vec![
                            ("Filters", scenario::list(vec![
                                scenario::map(vec![
                                    ("Key", scenario::lit(::serde_json::json!("name"))),
                                    ("Values", scenario::list(vec![
                                        scenario::name("s"),
                                    ])),
                                ]),
                            ])),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_secrets()
                                        .filters(
                                            aws_sdk_secretsmanager::types::Filter::builder()
                                                .key(aws_sdk_secretsmanager::types::FilterNameStringType::from("name"))
                                                .values(b.string("Filters[0].Values[0]")?)
                                                .build()
                                        )
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.SecretList",
                    vec![
                        scenario::where_entry("$.ARN", scenario::context("secret.arn")),
                    ],
                ),
            ),
        ],
    }
}

fn test_secretsmanager_gen_secret_describe_secret(client: &aws_sdk_secretsmanager::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeSecret",
            params: scenario::map(vec![
                ("SecretId", scenario::context("secret.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_secret()
                            .secret_id(b.string("SecretId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.ARN", scenario::context("secret.arn")),
            ]),
        ],
    }
}

fn test_secretsmanager_gen_secret_update_secret(client: &aws_sdk_secretsmanager::Client) -> Test {
    Test {
        call: Call {
            op: "UpdateSecret",
            params: scenario::map(vec![
                ("Description", scenario::lit(::serde_json::json!("compat scenario secret, updated"))),
                ("SecretId", scenario::context("secret.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .update_secret()
                            .description("compat scenario secret, updated")
                            .secret_id(b.string("SecretId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(10, 1000,
                scenario::readback(
                    Call {
                        op: "DescribeSecret",
                        params: scenario::map(vec![
                            ("SecretId", scenario::context("secret.arn")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .describe_secret()
                                        .secret_id(b.string("SecretId")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.Description", scenario::lit(::serde_json::json!("compat scenario secret, updated"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_secretsmanager_gen_secret_tag_resource(client: &aws_sdk_secretsmanager::Client) -> Test {
    Test {
        call: Call {
            op: "TagResource",
            params: scenario::map(vec![
                ("SecretId", scenario::context("secret.arn")),
                ("Tags", scenario::lit(::serde_json::json!([{"Key": "compat", "Value": "scenario"}]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .tag_resource()
                            .secret_id(b.string("SecretId")?)
                            .tags(
                                aws_sdk_secretsmanager::types::Tag::builder()
                                    .key("compat")
                                    .value("scenario")
                                    .build()
                            )
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(10, 1000,
                scenario::list_contains(
                    Some(Call {
                        op: "DescribeSecret",
                        params: scenario::map(vec![
                            ("SecretId", scenario::context("secret.arn")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .describe_secret()
                                        .secret_id(b.string("SecretId")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Tags",
                    vec![
                        scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("compat"))),
                        scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("scenario"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_secretsmanager_gen_secret_untag_resource(client: &aws_sdk_secretsmanager::Client) -> Test {
    Test {
        call: Call {
            op: "UntagResource",
            params: scenario::map(vec![
                ("SecretId", scenario::context("secret.arn")),
                ("TagKeys", scenario::lit(::serde_json::json!(["compat"]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .untag_resource()
                            .secret_id(b.string("SecretId")?)
                            .tag_keys("compat")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(10, 1000,
                scenario::absent_from_list(
                    Some(Call {
                        op: "DescribeSecret",
                        params: scenario::map(vec![
                            ("SecretId", scenario::context("secret.arn")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .describe_secret()
                                        .secret_id(b.string("SecretId")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Tags",
                    vec![
                        scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("compat"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_secretsmanager_gen_secret_get_random_password(client: &aws_sdk_secretsmanager::Client) -> Test {
    Test {
        call: Call {
            op: "GetRandomPassword",
            params: scenario::lit(::serde_json::json!({"ExcludePunctuation": true, "PasswordLength": 24})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_random_password()
                            .exclude_punctuation(true)
                            .password_length(24)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::matches("$.RandomPassword", "^[A-Za-z0-9]{24}$"),
            ]),
        ],
    }
}

fn test_secretsmanager_gen_secret_batch_get_secret_value(client: &aws_sdk_secretsmanager::Client) -> Test {
    Test {
        call: Call {
            op: "BatchGetSecretValue",
            params: scenario::map(vec![
                ("SecretIdList", scenario::list(vec![scenario::context("secret.arn")])),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .batch_get_secret_value()
                            .secret_id_list(b.string("SecretIdList[0]")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.SecretValues",
                vec![
                    scenario::where_entry("$.ARN", scenario::context("secret.arn")),
                    scenario::where_entry("$.SecretString", scenario::lit(::serde_json::json!("compat-scenario-initial"))),
                ],
            ),
        ],
    }
}

fn test_secretsmanager_gen_secret_validate_resource_policy(client: &aws_sdk_secretsmanager::Client) -> Test {
    Test {
        call: Call {
            op: "ValidateResourcePolicy",
            params: scenario::map(vec![
                ("ResourcePolicy", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Principal\":{\"AWS\":\"arn:aws:iam::123456789012:root\"},\"Action\":\"secretsmanager:GetSecretValue\",\"Resource\":\"*\"}]}"))),
                ("SecretId", scenario::context("secret.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .validate_resource_policy()
                            .resource_policy("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Principal\":{\"AWS\":\"arn:aws:iam::123456789012:root\"},\"Action\":\"secretsmanager:GetSecretValue\",\"Resource\":\"*\"}]}")
                            .secret_id(b.string("SecretId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.PolicyValidationPassed", scenario::lit(::serde_json::json!(true))),
            ]),
        ],
    }
}

fn test_secretsmanager_gen_secret_put_resource_policy(client: &aws_sdk_secretsmanager::Client) -> Test {
    Test {
        call: Call {
            op: "PutResourcePolicy",
            params: scenario::map(vec![
                ("ResourcePolicy", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Principal\":{\"AWS\":\"arn:aws:iam::123456789012:root\"},\"Action\":\"secretsmanager:GetSecretValue\",\"Resource\":\"*\"}]}"))),
                ("SecretId", scenario::context("secret.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .put_resource_policy()
                            .resource_policy("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Principal\":{\"AWS\":\"arn:aws:iam::123456789012:root\"},\"Action\":\"secretsmanager:GetSecretValue\",\"Resource\":\"*\"}]}")
                            .secret_id(b.string("SecretId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(10, 1000,
                scenario::readback(
                    Call {
                        op: "GetResourcePolicy",
                        params: scenario::map(vec![
                            ("SecretId", scenario::context("secret.arn")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_resource_policy()
                                        .secret_id(b.string("SecretId")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.ARN", scenario::context("secret.arn")),
                        scenario::matches("$.ResourcePolicy", "secretsmanager:GetSecretValue"),
                    ],
                ),
            ),
        ],
    }
}

fn test_secretsmanager_gen_secret_get_resource_policy(client: &aws_sdk_secretsmanager::Client) -> Test {
    Test {
        call: Call {
            op: "GetResourcePolicy",
            params: scenario::map(vec![
                ("SecretId", scenario::context("secret.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_resource_policy()
                            .secret_id(b.string("SecretId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.ARN", scenario::context("secret.arn")),
                scenario::matches("$.ResourcePolicy", "secretsmanager:GetSecretValue"),
            ]),
        ],
    }
}

fn test_secretsmanager_gen_secret_delete_resource_policy(client: &aws_sdk_secretsmanager::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteResourcePolicy",
            params: scenario::map(vec![
                ("SecretId", scenario::context("secret.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_resource_policy()
                            .secret_id(b.string("SecretId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(10, 1000,
                scenario::readback(
                    Call {
                        op: "GetResourcePolicy",
                        params: scenario::map(vec![
                            ("SecretId", scenario::context("secret.arn")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_resource_policy()
                                        .secret_id(b.string("SecretId")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::missing("$.ResourcePolicy"),
                    ],
                ),
            ),
        ],
    }
}

fn test_secretsmanager_gen_secret_cancel_rotate_secret(client: &aws_sdk_secretsmanager::Client) -> Test {
    Test {
        call: Call {
            op: "CancelRotateSecret",
            params: scenario::map(vec![
                ("SecretId", scenario::context("secret.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .cancel_rotate_secret()
                            .secret_id(b.string("SecretId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.ARN", scenario::context("secret.arn")),
                scenario::equals("$.Name", scenario::name("s")),
            ]),
        ],
    }
}

fn test_secretsmanager_gen_secret_list_secrets(client: &aws_sdk_secretsmanager::Client) -> Test {
    Test {
        call: Call {
            op: "ListSecrets",
            params: scenario::map(vec![
                ("Filters", scenario::list(vec![
                    scenario::map(vec![
                        ("Key", scenario::lit(::serde_json::json!("name"))),
                        ("Values", scenario::list(vec![scenario::name("s")])),
                    ]),
                ])),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_secrets()
                            .filters(
                                aws_sdk_secretsmanager::types::Filter::builder()
                                    .key(aws_sdk_secretsmanager::types::FilterNameStringType::from("name"))
                                    .values(b.string("Filters[0].Values[0]")?)
                                    .build()
                            )
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.SecretList",
                vec![
                    scenario::where_entry("$.ARN", scenario::context("secret.arn")),
                ],
            ),
        ],
    }
}

fn test_secretsmanager_gen_secret_delete_secret(client: &aws_sdk_secretsmanager::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteSecret",
            params: scenario::map(vec![
                ("ForceDeleteWithoutRecovery", scenario::lit(::serde_json::json!(true))),
                ("SecretId", scenario::context("secret.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_secret()
                            .force_delete_without_recovery(true)
                            .secret_id(b.string("SecretId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(10, 1000,
                scenario::absent_by_error(
                    Call {
                        op: "DescribeSecret",
                        params: scenario::map(vec![
                            ("SecretId", scenario::context("secret.arn")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .describe_secret()
                                        .secret_id(b.string("SecretId")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    scenario::error("ResourceNotFoundException", "ResourceNotFoundException"),
                ),
            ),
        ],
    }
}

fn setup_secretsmanager_gen_version(client: &aws_sdk_secretsmanager::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreateSecret",
            params: scenario::map(vec![
                ("Description", scenario::lit(::serde_json::json!("compat scenario secret"))),
                ("Name", scenario::name("s")),
                ("SecretString", scenario::lit(::serde_json::json!("compat-scenario-initial"))),
            ]),
            export: vec![
                ("secret.arn", "$.ARN"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_secret()
                            .description("compat scenario secret")
                            .name(b.string("Name")?)
                            .secret_string("compat-scenario-initial")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_secretsmanager_gen_version(client: &aws_sdk_secretsmanager::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteSecret",
            params: scenario::map(vec![
                ("ForceDeleteWithoutRecovery", scenario::lit(::serde_json::json!(true))),
                ("SecretId", scenario::context("secret.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_secret()
                            .force_delete_without_recovery(true)
                            .secret_id(b.string("SecretId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_secretsmanager_gen_version_put_secret_value(client: &aws_sdk_secretsmanager::Client) -> Test {
    Test {
        call: Call {
            op: "PutSecretValue",
            params: scenario::map(vec![
                ("SecretId", scenario::context("secret.arn")),
                ("SecretString", scenario::lit(::serde_json::json!("compat scenario version value"))),
            ]),
            export: vec![
                ("version.id", "$.VersionId"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .put_secret_value()
                            .secret_id(b.string("SecretId")?)
                            .secret_string("compat scenario version value")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.VersionId"),
            ]),
            scenario::readback(
                Call {
                    op: "GetSecretValue",
                    params: scenario::map(vec![
                        ("SecretId", scenario::context("secret.arn")),
                        ("VersionId", scenario::context("version.id")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .get_secret_value()
                                    .secret_id(b.string("SecretId")?)
                                    .version_id(b.string("VersionId")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.VersionId", scenario::context("version.id")),
                ],
            ),
            scenario::list_contains(
                Some(Call {
                    op: "ListSecretVersionIds",
                    params: scenario::map(vec![
                        ("SecretId", scenario::context("secret.arn")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_secret_version_ids()
                                    .secret_id(b.string("SecretId")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.Versions",
                vec![
                    scenario::where_entry("$.VersionId", scenario::context("version.id")),
                ],
            ),
        ],
    }
}

fn test_secretsmanager_gen_version_get_secret_value(client: &aws_sdk_secretsmanager::Client) -> Test {
    Test {
        call: Call {
            op: "GetSecretValue",
            params: scenario::map(vec![
                ("SecretId", scenario::context("secret.arn")),
                ("VersionId", scenario::context("version.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_secret_value()
                            .secret_id(b.string("SecretId")?)
                            .version_id(b.string("VersionId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.VersionId", scenario::context("version.id")),
            ]),
        ],
    }
}

fn test_secretsmanager_gen_version_update_secret_version_stage(client: &aws_sdk_secretsmanager::Client) -> Test {
    Test {
        call: Call {
            op: "UpdateSecretVersionStage",
            params: scenario::map(vec![
                ("MoveToVersionId", scenario::context("version.id")),
                ("SecretId", scenario::context("secret.arn")),
                ("VersionStage", scenario::lit(::serde_json::json!("COMPATSCENARIO"))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .update_secret_version_stage()
                            .move_to_version_id(b.string("MoveToVersionId")?)
                            .secret_id(b.string("SecretId")?)
                            .version_stage("COMPATSCENARIO")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.ARN", scenario::context("secret.arn")),
            ]),
            scenario::readback(
                Call {
                    op: "GetSecretValue",
                    params: scenario::map(vec![
                        ("SecretId", scenario::context("secret.arn")),
                        ("VersionStage", scenario::lit(::serde_json::json!("COMPATSCENARIO"))),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .get_secret_value()
                                    .secret_id(b.string("SecretId")?)
                                    .version_stage("COMPATSCENARIO")
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.SecretString", scenario::lit(::serde_json::json!("compat scenario version value"))),
                    scenario::equals("$.VersionId", scenario::context("version.id")),
                ],
            ),
        ],
    }
}

fn test_secretsmanager_gen_version_list_secret_version_ids(client: &aws_sdk_secretsmanager::Client) -> Test {
    Test {
        call: Call {
            op: "ListSecretVersionIds",
            params: scenario::map(vec![
                ("SecretId", scenario::context("secret.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_secret_version_ids()
                            .secret_id(b.string("SecretId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Versions",
                vec![
                    scenario::where_entry("$.VersionId", scenario::context("version.id")),
                ],
            ),
        ],
    }
}
