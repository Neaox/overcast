* [cloudwatch/web] the log stream viewer loads history older than its window on demand: nearing the oldest edge of loaded data extends the time window backward in doubling chunks (FilterLogEvents pages forward only, so "older" is time-window expansion), prepending without a viewport jump and stopping honestly — "Start of logs" when history is exhausted (the streams' firstEventTimestamp metadata bounds the search, no empty-window probing), "Start of range" when a user-set start time is the floor, "Loading older events…" in flight; a search result's anchored arrival inherits it above the 15-minute lead-in, and a new jump-to-timestamp toolbar control repositions the view on the first event at or after a chosen time via the same anchor machinery
