~ [web/ci] the console's server-side TypeScript types (health, metrics, debug metrics, advisories, event stream, diagnostics provenance) are now generated from the Go response structs by `cmd/tsgen` into `web/src/types/api.gen.ts`, and `make check-ts` fails CI when the two drift — a Go field rename can no longer ship a console reading `undefined`
