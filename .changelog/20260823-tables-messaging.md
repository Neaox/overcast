~ [web] the SQS, SNS, Kinesis, MSK and Pipes list pages sort by clicking a column
  header — name on every page, plus message counts and visibility timeout on
  Queues, shards and retention on Streams, brokers on Clusters and creation time
  on Pipes. The Queues page also gains a Columns menu for hiding the ARN and
  timeout columns.
~ [web] the sub-tables on the SQS queue, SNS topic, Kinesis stream, Pipes pipe
  and EventBridge bus detail pages sort the same way, and tell "nothing here
  yet" apart from "nothing matches your filter" on the bus page's rule list.
