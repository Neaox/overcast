* [cloudwatch/web] token highlight ranges are now static rather than live, so a busy tail no longer pays range-bookkeeping on every row the virtualizer mounts or removes — the engine stops fixing up tens of thousands of live range boundaries per DOM change
* [cloudwatch/web] scrolling deep through a highlighted stream no longer locks Firefox up or blanks out — scrolling now mutates the token-highlight sets not at all: unmounting rows leave visually-inert garbage collected by one lazy sweep, hydration adds only the newly settled rows, and the per-range delete loop a 31-second trace showed consuming 15 seconds is gone entirely
