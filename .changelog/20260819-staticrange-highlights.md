* [cloudwatch/web] token highlight ranges are now static rather than live, so a busy tail no longer pays range-bookkeeping on every row the virtualizer mounts or removes — the engine stops fixing up tens of thousands of live range boundaries per DOM change
* [cloudwatch/web] scrolling deep through a highlighted stream no longer locks Firefox up — token highlights are rebuilt and swapped in wholesale instead of deleted range-by-range from the page-global sets, whose per-mutation cost in Firefox grew with set size until a 31-second trace showed 15 seconds spent inside the delete loop
