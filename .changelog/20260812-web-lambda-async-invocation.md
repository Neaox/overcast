+ [web/lambda] a function's Configuration tab now has an **Asynchronous invocation** section showing its dead-letter target, with an Edit that sets or clears one — the console half of `DeadLetterConfig`, which until now could only be set from an SDK or a template. It states the retry policy rather than offering fields for it: a failed invocation is retried twice, and `MaximumRetryAttempts` and `MaximumEventAgeInSeconds` are not configurable because `PutFunctionEventInvokeConfig` is not emulated. Inputs the emulator would accept and ignore would be worse than the sentence
