~ [web] resource list tables sort by clicking a column header, and hide columns
  from a **Columns** menu. Sorting is deep-linkable on the KMS, SSM, AppSync and
  Step Functions pages — `?sort=name` / `?sort=-name` alongside the existing `?q=` — so a
  sorted view survives a reload and can be shared.
