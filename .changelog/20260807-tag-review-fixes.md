* [waf,stepfunctions,shield,glue,elbv2,rds,serviceutil] fix tag validation gaps and dead code: WAF no longer publishes spurious creation events on tag writes; Step Functions typed path validates tags; Shield and ELBv2 now validate tag limits; Glue uses shared tag helpers; RDS fixes unbounded loop in tag parameter parsing; removed dead TaggedResource and shieldTagsToList