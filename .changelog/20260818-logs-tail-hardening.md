* [cloudwatch/web] the log tail infrastructure now survives the ways a session actually dies: a heartbeat staleness watchdog marks a tail dead when frames stop arriving without an error (machine sleep, NAT timeout — ~10s of silence against the emulator's 1s heartbeat), the map's log peek keeps the newest edge reachable after a tail death by walking GetLogEvents' forward tokens while the user sits at the bottom (reconciled against what the dead session already delivered, so nothing shows twice), and @tanstack/react-virtual is bumped to 3.14.9 for its iOS/touch-aware deferred scroll adjustments around the peek's prepend anchoring
