* [cli] `overcast status` probed `/health`, a path the daemon does not serve, so it reported an error against a healthy daemon — it checks `/_overcast/health` and enriches its one-liner with the version and storage backend
