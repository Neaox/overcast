* [cloudwatch/web] five display QOL features on the log stream viewer, mirroring the AWS console where it has the feature: a UTC/local timestamp toggle covering rows and the time-range chip; opt-in inter-row time deltas beside the Time column, devtools-style; view preferences (Table/Plaintext, Format, Syntax, Wrap, sort direction, UTC, Deltas, Collapse) that survive revisits via localStorage and fall back to the defaults silently when the stored state is corrupt; an opt-in Collapse mode rendering every row as one fixed-height truncated line — badges, tints and ANSI colour intact — with click-to-expand per row, which also removes the virtualizer's measurement work for collapsed rows; and an Export control downloading the currently loaded events in display order as CSV (the console's search-results shape: ISO 8601 timestamps, raw message bytes) or JSON, saying so explicitly when more events remain unloaded on the server
