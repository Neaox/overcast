*. [eks] fixes a shutdown race in `CreateCluster`'s live-mode bootstrap: it called `liveWg.Add(1)` without checking `liveStopping` under `liveLifecycleMu` first, unlike `recoverLiveCluster` — the same `sync.WaitGroup` contract violation as #1282/#1290, reachable via a create racing `Stop`, and able to leave a k3s container starting after shutdown; a create that lands after `Stop` has begun is now refused, leaving the cluster record `FAILED` with a reason instead of a half-started container (#1291)
