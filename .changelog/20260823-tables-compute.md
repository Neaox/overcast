~ [web] the Lambda function and layer lists, the ECS cluster list, the Auto
  Scaling group list and the Applications list sort by clicking a column header
  — by name, and by the counts and timestamps each page carries (memory,
  timeout, running tasks, capacity, latest version, created). The ECS cluster
  list and the Auto Scaling group list also gain the **Columns** menu, so their
  wider tables can be narrowed to the columns being read.
~ [web] the tables inside the Lambda layer, ECS cluster, Applications, EC2
  instance and EC2 VPC detail pages sort the same way — versions, tasks,
  services, container instances, subnets, route tables, endpoints, security
  groups and tags — and now show the app's skeleton rows while loading and its
  standard empty state when there is nothing to list, instead of a bare spinner.
