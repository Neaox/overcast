+ [docs] documented every `overcast` CLI subcommand (start/stop/restart/status/wait/logs/services/reset/config/env/aws/import/bridge/https/trust/mcp) in a new CLI reference page, linked from the README and docs index
~ [docs] docker run examples now pull `:latest` instead of `:alpha`, matching the moving `:latest` tag every build now publishes
