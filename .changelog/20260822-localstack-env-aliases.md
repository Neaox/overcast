+ [config] LocalStack's `EDGE_PORT`, `GATEWAY_LISTEN`, `DEFAULT_REGION`, `DATA_DIR`, `DEBUG`, `PERSISTENCE`, `HOSTNAME_EXTERNAL`, and `LAMBDA_RUNTIME_ENVIRONMENT_TIMEOUT` are now accepted as compatibility aliases for the matching `OVERCAST_*`/`LAMBDA_*` setting (#1190), completing the drop-in-replacement audit started by the `LOCALSTACK_HOST` alias; `SERVICES`, `LOCALSTACK_API_KEY`, and `LOCALSTACK_AUTH_TOKEN` are recognised and logged once at startup as inert rather than silently ignored; every alias shares one conflict rule (disagreeing values fail startup naming both) and one shared table-driven mechanism (internal/config/localstack_aliases.go)
+ [docs] docs/migration-from-localstack.md documents which further LocalStack variables were deliberately not aliased and why (LAMBDA_DOCKER_NETWORK, LAMBDA_KEEPALIVE_MS, the CORS variables, EAGER_SERVICE_LOADING, SNAPSHOT_*), rather than half-mapping them
