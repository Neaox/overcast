* [cloudwatch/web] three interaction QOL features on the log stream viewer: a hover affordance on any row carrying a Lambda request id (START/END/REPORT lines, JSON `requestId` fields, the Node runtime's tab-separated UUID column) that filters the view to that invocation — it fills the filter with the id as a quoted term, exactly the FilterLogEvents search typing it would run, no new query semantics; a per-row copy-link control yielding an absolute URL that reopens the stream anchored on exactly that event, the same `anchorTs`/`anchorSig` contract search-hit deep links already use; and keyboard row navigation when the list has focus — ArrowDown/ArrowUp or j/k move a visible cursor, Enter expands or folds the focused row in Collapse mode, c copies its plain message — announced via `aria-activedescendant`, never touching keys typed into inputs or the Ctrl/Cmd+F filter shortcut, and costing no re-render of row content on cursor moves
