+ [stepfunctions/eventbridge] Step Functions standard-workflow execution status transitions (StartExecution's initial RUNNING record, the SUCCEEDED/FAILED/TIMED_OUT/ABORTED terminal write, and StopExecution's direct ABORTED write) now publish an AWS-shaped `Step Functions Execution Status Change` event onto the default EventBridge bus, so a rule matching one actually fires; EXPRESS workflows are excluded, matching AWS (#758, #1221)
