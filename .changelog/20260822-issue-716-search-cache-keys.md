* [web] global search now hits the query cache instead of refetching on every keystroke — the s3, sqs, sns, kinesis, lambda, secretsmanager, and logs contributors built their cache-lookup key as [service, resource, baseUrl] (logs omitted the endpoint entirely), which never matched the [baseUrl, region, service, resource] shape the real feature queries use, so the lookup always missed and every search ignored the selected region (#716)
