+ [router] 9 operations newly modeled by AWS are recognised, spanning 1 service new to the corpus (`iam-toolbox`). A signed request to one reaches a protocol-correct `501` marked `x-emulator-unsupported`, in that service's own error envelope, instead of falling through to the S3 fallback and coming back as a bucket or object answer
