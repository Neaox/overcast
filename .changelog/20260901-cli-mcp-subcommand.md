+ [cli] `overcast mcp` serves the workspace MCP server from the unified binary
-. [cli] the standalone `overcast-mcp` binary — never shipped in a GitHub release; local tooling (Makefile, devcontainer, `.vscode/mcp.json`) now builds and runs `overcast mcp` instead
