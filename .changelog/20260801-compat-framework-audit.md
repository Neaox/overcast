- [compat] framework audit before suite expansion: eight node-js-sdk assertions could never fail (`.some(thisArg)` misuse + `notStrictEqual` against the message string) and are repaired — all eight pass against the current emulator, so the fix is pure coverage; `sts-assume` no longer references the role a sibling parallel group deletes; the parity checker is now bidirectional, so a suite cannot ship tests the registry does not know (cdk's two filtered-ESM tests are registered)
