* [cloudformation] an update rollback honours `DeletionPolicy: Retain` on the resources that update created, instead of deleting them regardless — a failed update no longer destroys a resource the template asked to keep, and `RetainExceptOnCreate` on the operation opts back into deleting it, as on the create path. The replacement a failed update created is still deleted: the original is what the stack rolls back to
