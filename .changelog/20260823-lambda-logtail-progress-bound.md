* [lambda] a cold-started invocation's `X-Amz-Log-Result` tail no longer drops the handler's own output when the Docker log stream takes longer than 100 ms to connect, or hands the first bytes over late, under daemon contention; the tail wait's bound now runs from the last observed pipeline event — connect answered, reader parked, bytes read, line appended — rather than from the moment the wait began, holds for a first connect that is still in flight, and keeps one absolute 1 s ceiling so a wedged pipeline can still never hold up the response
