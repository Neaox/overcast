-. [web] the standalone Node entry point for the dev Hono BFF (`web/api/src/node.ts`) and the `@overcast/api` package manifest around it (`web/api/package.json`, its own `pnpm-lock.yaml`, the `dev`/`build`/`start` scripts, and `@hono/node-server`) — a leftover from before the web console moved into the Go binary; nothing installed, built, or started any of it, and `web/api/src` is typechecked, tested, and resolved from `web/` as plain source files
