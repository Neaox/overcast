~ [web] the Map page's topology graph, the Inbox's captured messages and the request-tracing payloads (trace detail/list/count/search) join the generated TypeScript types in `web/src/types/api.gen.ts` — their hand-written mirrors are gone, and `trace.Entry`/`Hop` now marshal through package-level wire structs that `cmd/tsgen` renders (the generator will not render a type that has its own `MarshalJSON`, so a struct can no longer be described as a shape it never sends)
