* [s3/web] the object preview highlights through the same zero-DOM kernel as the log views: where the browser has the CSS Custom Highlight API (Chrome 105+, Safari 17.2+, Firefox 140+) a highlighted JSON, XML/HTML, CSS, or JavaScript preview renders as one text node with token colours painted as ranges — no per-token spans for extensions and the accessibility tree to walk — and elsewhere the Prism markup rendering is unchanged; the over-256-KiB "shown as plain text; too large to format" behaviour is kept
