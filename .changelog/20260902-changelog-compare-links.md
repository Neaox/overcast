* [release] the CHANGELOG version compare links point at `github.com/overcast-sh`, so every one of them resolves.
  they still pointed at the pre-rename owner, which also failed the release gate that checks them.
