* [docker] a registry 5xx or a deadline/timeout/reset while an image pull was in flight no longer permanently fails the container-backed create that triggered it — the shared image puller now retries these alongside the rate-limit shapes PR #1162 already handled, within the same bounded 3-attempt backoff (~2.5s total across at most 4 attempts); a caller's own context ending (shutdown, or its own deadline) still stops the pull immediately rather than retrying
