service: pipes
status: in_progress
priority: p2
reference_policy: aws_docs_only_unless_user_approves_real_aws
last_updated: 2026-08-22
protocols: [rest_json]
implementation: [internal/services/pipes]
integration_tests: [tests/integration/pipes]
capability_docs: docs/services/pipes.md
operation_count_estimate: 5 supported
current_support_level: Pipe CRUD supported. Sources: DynamoDB Streams, Kinesis, SQS. Optional Lambda enrichment. Targets: Lambda, SQS, SNS, Step Functions, Kinesis, Firehose, EventBridge bus. Unsupported combinations are rejected at CreatePipe/UpdatePipe.
operations: {}
findings:
  fixed:
    - Sources beyond DynamoDB Streams (Kinesis, SQS) and targets beyond SQS (#470).
    - Optional Lambda enrichment between source and target (#470).
    - Unsupported source/target/enrichment combinations now fail at CreatePipe/UpdatePipe instead of being stored and ignored (#470).
    - CreatePipe returns AWS's 200 with the six-member response body (#470).
    - Reconciled the stale service comment about UpdatePipe.
    - A Lambda target's batchItemFailures report is honoured for SQS, Kinesis and DynamoDB Streams sources (#512).
    - A Kinesis source's MaximumRetryAttempts and DeadLetterConfig are honoured, matching the DynamoDB Streams behaviour #582 already shipped; a DeadLetterConfig naming a destination that is not an SQS queue or SNS topic is refused at CreatePipe/UpdatePipe time (#513).
  remaining_gaps:
    - FilterCriteria is rejected rather than evaluated; a pattern matcher would need the EventBridge one extracted.
    - MaximumBatchingWindowInSeconds and ParallelizationFactor are not emulated.
    - Partial-batch failure reporting is not read from a Step Functions target (async StartExecution has no response) or from a Lambda enrichment (whose return value replaces the batch).
    - LogConfiguration and KmsKeyIdentifier are accepted but have no effect.
  intentionally_partial:
    - RoleArn is required but never evaluated - Overcast is not a security boundary.
handoff:
  start_with: [FilterCriteria evaluation, batching window]
  do_not_redo: [source/target wiring, enrichment, loud rejection of unsupported combinations]
  commands: [go test -count=1 ./internal/services/pipes/... ./tests/integration/pipes/...]
