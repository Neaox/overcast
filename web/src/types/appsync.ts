export type {
  GraphqlApi as AppSyncApi,
  DataSource as AppSyncDataSource,
  Resolver as AppSyncResolver,
  FunctionConfiguration as AppSyncFunction,
  ApiKey as AppSyncApiKey,
  Type as AppSyncType,
} from "@aws-sdk/client-appsync"
