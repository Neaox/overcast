### Did not waive the changelog

$PROBLEM

Nothing changed: this PR still owes a fragment or a reason. Usage:
`/no-changelog <reason>` — for example
`/no-changelog test-only: new fixtures for the SQS integration suite`.
`/needs-changelog` puts the question back once it has been waived.
