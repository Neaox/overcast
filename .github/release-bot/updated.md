### Updated for `main`

I merged `$SHORT_SHA` into this branch and added the entries below to
`## [$VERSION]` in `CHANGELOG.md`, deleting the fragment files they came from.

**Nothing needed from you** — this PR is still mergeable. Reword the new bullets
whenever it suits; I only ever append.
