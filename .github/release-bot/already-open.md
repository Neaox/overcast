### Nothing to prepare

The release PR for **$VERSION** is already open, and I keep it current on every
push to `main` — new entries are folded into `## [$VERSION]` automatically, so
there is no need to re-run release prep.

If you want the section **rebuilt from scratch**, discarding any wording changed
by hand, re-run with `regenerate: true`.
