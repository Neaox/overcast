### Hold lifted

The release this PR was waiting on is no longer in flight, so breaking changes
are welcome on `main` again. I re-ran the hold check and it passed — nothing to
re-push, and nothing else is needed from you.
