### Did not retarget

$REASON

Nothing changed: this PR still targets `$CURRENT_BASE`. Usage:
`/retarget <branch>` — for example `/retarget v2.0.0`. I create the branch off
`main` if it does not exist yet.
